Apply the debug-ui skill. Use the browser MCP to navigate to my running UI, read the console, and fix errors.

URL: [e.g. http://localhost:5183] What broke: [optional — e.g. "selected instruments-service, opened Deploy tab"]
