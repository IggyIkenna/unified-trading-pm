name: Quality Gates

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-gates:
    uses: IggyIkenna/unified-trading-pm/.github/workflows/infra-quality-gates.yml@live-defi-rollout
    with:
      repo_name: unified-trading-pm
      run_quality_gates_sh: true
      run_basedpyright: true
      run_actionlint: true
      run_flow_coverage: true
      sibling_deps: "unified-trading-library unified-internal-contracts"
    secrets: inherit
