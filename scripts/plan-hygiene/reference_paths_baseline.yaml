# Baseline for check_reference_paths.py — the /plans/... + /codex/... reference
# path convention (operator ruling 2026-07-23).
#
# This is a SHRINKING ratchet. format_count/existence_count are the number of
# pre-existing violations the gate tolerates. A live count EXCEEDING its baseline
# fails the check — a NEW violation landed; fix the reference, don't grow the
# number. LOWER a count (re-run --update-baseline) the moment violations are fixed.
# NEVER hand-raise a count.
#
# SSOT: codex/11-project-management/cross-reference-path-convention.md,
# plans/active/issues/reference_path_convention_2026_07_23.md.
note: >-
  Seeded 2026-07-23 pre-migration (12,021 / 630), then corrected same day once the corpus-wide reformat
  (scripts/plan-hygiene/fix_reference_paths.py, ~2,150 files) landed as its own follow-up commit: format_count drops to
  176 (the ratchet's whole point). existence_count RISES to 1,285 — not a regression: most refs weren't in the checkable
  leading-slash form at all pre-migration, so the pre-migration 630 undercounted how many are actually dangling;
  completing the migration measures the corpus honestly for the first time, it doesn't create new breakage. See
  plans/active/issues/reference_path_convention_2026_07_23.md for the full cleanup backlog (both counts' remaining
  entries).
format_count: 176
existence_count: 1285
