# Repos whose quality-gates-v2 real test/lint job (qg-slices) runs on self-hosted runners
# instead of ubuntu-latest. SOURCE OF TRUTH for rollout-workflow-templates.sh's
# get_qg_runner_labels() — see that function + quality-gates-v2.yml.tmpl's own comment.
#
# HARD RULE: a repo goes on this list ONLY after its own self-hosted runner pool is
# registered + verified healthy (setup-glue-runners.sh POOL_TAG=<repo> ... install, then
# `status` shows the runner(s) online via the live GitHub API listing). Adding a repo here
# before its pool exists hangs that repo's promotion gate forever (no matching runner ever
# appears). github_actions_operator_gated_followups_2026_07_17.md.
#
# One repo name per line, exact match against workspace-manifest.json repo names.
# TRIMMED 2026-07-28 (fleet_wide_qg_self_hosted_runner_capacity_crisis_2026_07_27.md, P0 SCRIPT fix): the Phase-7
# fanout landed 19-24 repos on this list, each with only its own lone 1-runner pool, ALL colocated on the same
# shared 16-vCPU/64GB EC2 instance (i-0c9b283b31d6b5ca7) that also hosts PM's + agent-orchestrator's pools.
# That is >20 runner processes fighting over 16 vCPUs — the HARD RULE above ("verified healthy" pool) was never
# actually met for any of the removed entries. 9+ independent per-repo whack-a-mole reverts already happened
# overnight; this trims the SOURCE so future rollout-workflow-templates.sh runs can't re-arm it. Re-add a repo
# here only after it has its own dedicated, capacity-planned pool (not a lone runner sharing the fleet box) —
# see that issue doc's still-open [OPERATOR] P0 fleet-posture + [DATA]/[VERIFY] capacity-planning todos.
agent-orchestrator
unified-trading-pm
