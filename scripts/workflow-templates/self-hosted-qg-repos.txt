# Repos whose quality-gates-v2 real test/lint job (qg-slices) runs on self-hosted runners
# instead of ubuntu-latest. SOURCE OF TRUTH for rollout-workflow-templates.sh's
# get_qg_runner_labels() — see that function + quality-gates-v2.yml.tmpl's own comment.
#
# HARD RULE: a repo goes on this list ONLY after its own self-hosted runner pool is
# registered + verified healthy (setup-glue-runners.sh POOL_TAG=<repo> ... install, then
# `status` shows the runner(s) online via the live GitHub API listing). Adding a repo here
# before its pool exists hangs that repo's promotion gate forever (no matching runner ever
# appears). github_actions_operator_gated_followups_2026_07_17.md.
#
# One repo name per line, exact match against workspace-manifest.json repo names.
# TRIMMED then PARTIALLY RESTORED 2026-07-28 (fleet_wide_qg_self_hosted_runner_capacity_crisis_2026_07_27.md, P0):
# the Phase-7 fanout landed 19-24 repos on this list, each with only its own lone 1-runner pool, ALL colocated on
# the same shared 16-vCPU/64GB EC2 instance (i-0c9b283b31d6b5ca7). That caused hours-long CI stalls fleet-wide
# (9+ whack-a-mole per-repo reverts overnight). This file was briefly trimmed to just the two verified-adequate
# pools (agent-orchestrator, unified-trading-pm) as the systemic fix. **Operator ruling (2026-07-28, live):
# capacity has freed up materially since the incident — do not blanket-strip the fleet off self-hosted.** This
# list is restored to match the repos that are ACTUALLY self-hosted right now: the two original pools, the 5
# repos nobody had touched (their own CI never actually broke), and the 6 repos this session reverted-then-
# restored per that ruling. The ~10 repos other agents genuinely reverted to ubuntu-latest overnight for a real,
# observed hang (execution-service, batch-live-reconciliation-service, alerting-service, client-reporting-api,
# e2e-testing, unified-trading-library, ml-service, deployment-api, market-data-processing-service,
# deployment-service) are intentionally left OFF this list — that was settled, evidence-based work, not touched
# here. Re-add any of those only once it separately gets a real capacity-planned pool.
agent-orchestrator
unified-trading-pm
strategy-service
system-integration-tests
trading-agent-service
unified-api-contracts
unified-trading-api
features-service
fund-administration-service
greeks-service
ibkr-gateway-infra
instruments-service
market-tick-data-service
