# Repos whose quality-gates-v2 real test/lint job (qg-slices) runs on self-hosted runners
# instead of ubuntu-latest. SOURCE OF TRUTH for rollout-workflow-templates.sh's
# get_qg_runner_labels() — see that function + quality-gates-v2.yml.tmpl's own comment.
#
# HARD RULE: a repo goes on this list ONLY after its own self-hosted runner pool is
# registered + verified healthy (setup-glue-runners.sh POOL_TAG=<repo> ... install, then
# `status` shows the runner(s) online via the live GitHub API listing). Adding a repo here
# before its pool exists hangs that repo's promotion gate forever (no matching runner ever
# appears). github_actions_operator_gated_followups_2026_07_17.md.
#
# One repo name per line, exact match against workspace-manifest.json repo names.
# TRIMMED then PARTIALLY RESTORED 2026-07-28 (fleet_wide_qg_self_hosted_runner_capacity_crisis_2026_07_27.md, P0):
# the Phase-7 fanout landed 19-24 repos on this list, each with only its own lone 1-runner pool, ALL colocated on
# the same shared 16-vCPU/64GB EC2 instance (i-0c9b283b31d6b5ca7). That caused hours-long CI stalls fleet-wide
# (9+ whack-a-mole per-repo reverts overnight, root cause: the legacy token governor (qg-host-governor.sh) counts
# concurrent heavy phases blindly (K=4) with no idea of actual RAM, so N cheap repos + 1 heavy one still
# oversubscribed the host). This file was briefly trimmed to just the two verified-adequate pools
# (agent-orchestrator, unified-trading-pm), then partially restored same-day per operator ruling ("capacity has
# freed up, don't blanket-strip the fleet").
#
# FULLY RESTORED 2026-07-28 (same issue doc, proper fix): python-quality-gates-v2.yml now activates the RAM+CPU
# dual-gate reservation-mode governor (QG_GOVERNOR_MODE=reservation, f9ffe7424) whenever a caller passes
# self_hosted_runner_labels — admission is sized against each repo's MEASURED peak RSS
# (scripts/dev/qg_resource_baseline.json) instead of a blind token count, so a repo that doesn't fit the live RAM
# budget queues safely instead of crashing the host. Verified live (instruments-service run 30362525333:
# QG_MEM_CAP computed as this repo's measured baseline x1.2, not the token-mode 10G default). The 10 repos below
# were reverted overnight for exactly the blind-token oversubscription this fix addresses
# (execution-service, batch-live-reconciliation-service, alerting-service, client-reporting-api, e2e-testing,
# unified-trading-library, ml-service, deployment-api, market-data-processing-service, deployment-service) — all
# 10 still have their self-hosted runner pool registered + online (verified via the GitHub API), so no new pool
# setup is needed. Re-added now that admission is RAM-aware; if the host shows renewed contention, the governor's
# own queueing (not a repo-by-repo revert) is the first thing to check — see the issue doc.
#
# unified-trading-library REVERTED AGAIN 2026-07-28 (cicd agent, escalation agt-5fd0b2, ldr_qg_failure at
# 0a3f2036, 11th corroboration in the issue doc): this is NOT the queueing case the note above anticipates —
# `QG slice (tests)` was already ADMITTED (past the governor's gate) and then sat in_progress on
# "Run quality gates (leg tests)" for 3+ hours with zero step completion (run 30375127722), while the host itself
# showed `uptime` load average 90.8/80.5/81.8 on a 16-vCPU box and 13/15GB swap in use (87%) — i.e. severe
# whole-host thrashing, not a governor-admission miss. A separate run at the same commit also failed earlier with
# a `uv sync` "Failed to write to the distribution cache: No such file or directory" (a shared ~/.cache/uv path
# race under this level of host contention). Reservation-mode governor prevents an oversized admission from
# CRASHING the host; it does not prevent an admitted job from starving once the host is already this saturated
# from other concurrent load (co-resident AO agent slot-worker sessions + other self-hosted runner jobs). Reverted
# to ubuntu-latest per the doc's own precedented per-repo fix (10 prior instances); re-adding needs either the host
# load easing durably or a tighter admission cap, not just re-flipping this line.
#
# e2e-testing REVERTED AGAIN 2026-07-29 (cicd agent slot-8, escalation agt-ce0e18, ldr_qg_failure at 3ec5c1c0,
# ldr-ci-monitor-detected): `QG slice (tests)` failed on a `pytest-timeout` (>60s) SIGALRM inside an in-memory,
# fixture-only test (test_smoke_runner.py::TestGreenOnRunnableForEachAg::test_one_runnable_shard_per_ag_smokes_green
# — no subprocess/IO, no code touching that file in the preceding 4 commits) that passes clean in 10.24s standalone
# locally. Same signature as every other corroboration in fleet_wide_qg_self_hosted_runner_capacity_crisis_2026_07_27.md
# — the reservation-mode governor prevents oversized ADMISSION from crashing the host, it does not stop an already-
# admitted job's SIGALRM budget being blown by whole-host contention. Reverted to ubuntu-latest per the doc's
# precedented per-repo fix; e2e-testing is NOT one of the operator's 6 explicitly-protected repos, so no ruling
# conflict.
#
# unified-trading-library + e2e-testing RE-ADDED 2026-08-05 (ci_pipeline_speed_and_cost_redesign_2026_08_05.md):
# both prior reverts' root cause was whole-host CPU/memory thrashing (load avg 90.8/80.5/81.8 on a 16-vCPU box,
# 87% swap for UTL; a SIGALRM timeout on an otherwise-clean 10s test for e2e-testing) — a DIFFERENT resource
# dimension from the disk-throughput saturation this session separately fixed (EBS 125->500 MB/s). Re-adding is
# justified by: (1) the disk fix removes one real contention source even though it wasn't THESE repos' documented
# cause, (2) a live host check at re-add time showed load average 4.08/4.92/6.24 (vs. the 90+ that caused the
# revert) and swap at 8.5/47Gi=18% (vs. 87%) — healthy headroom, not just "should be fine" hope. Both repos'
# self-hosted runner pools were already registered + verified online per the prior notes above (no new pool setup
# needed). This is a single-snapshot check, not a durably-proven fix for the underlying host-thrashing mechanism —
# if either repo's `QG slice (tests)` job shows the same starvation/SIGALRM signature again, revert per the same
# precedented per-repo playbook (this is the 2nd/3rd cycle for each, not a first attempt).
# 15 PUBLIC repos REMOVED 2026-08-05 (self_hosted_runner_public_repo_revert_2026_08_05.md, operator-confirmed
# intentionally public): system-integration-tests, trading-agent-service, unified-trading-library,
# unified-api-contracts, unified-trading-api, fund-administration-service, greeks-service, ibkr-gateway-infra,
# instruments-service, batch-live-reconciliation-service, alerting-service, client-reporting-api, deployment-api,
# market-data-processing-service, deployment-service. GitHub Actions on GitHub-hosted runners is UNMETERED for
# public repos regardless of minutes used — none of the self-hosted-migration docs above ever checked repo
# visibility before adding these. Reverting relieves the shared self-hosted VM's documented capacity contention
# (fleet_wide_qg_self_hosted_runner_capacity_crisis_2026_07_27.md) at zero billing cost. This is a DIFFERENT,
# permanent reason from unified-trading-library's/e2e-testing's own prior revert/re-add cycles above (those were
# about host contention, not billing) — do not fold this into that flaky-recurrence pattern.
#
# unified-trading-pm REMOVED 2026-08-07 (self_hosted_runner_public_repo_revert_2026_08_05.md todo 24):
# PM itself was flipped PUBLIC 2026-08-06 while still routing ~8 workflows to this pool — the same
# free-hosted-minutes-for-public-repos + fork-PR-security-exposure reasoning as the 17-repo revert
# above, now applied to PM's own workflows.
#
# instruments-service + unified-api-contracts + market-data-processing-service RE-ADDED 2026-08-14
# (operator-directed billing migration): all three were flipped PRIVATE first (verified via
# `gh repo view --json visibility`), THEN their self-hosted pools were (re-)installed on
# ci-escalation-runner-vm-1 (i-042a6332509482556) and confirmed `online` via the live GitHub API —
# private-before-self-hosted, the correct order per the fork-PR-RCE lesson above, never the reverse.
# Their pools already existed (dormant since the 2026-08-05 revert, disabled not deleted); the admin
# GH_PAT Secret Manager read failed once on ubuntu's ambient gcloud account (stale WIF token,
# unrelated to the per-repo isolated GLUE_GCLOUD_CONFIG dirs which were healthy) — fixed by resetting
# the active account, not by touching any repo's isolated config. GLUE_COUNT=1/WRITER_COUNT=0 each,
# matching every other non-PM/non-AO repo's pool shape. Remaining public repos with a dormant-but-
# still-installed pool from the same 2026-08-05 revert (system-integration-tests, trading-agent-service,
# unified-trading-library, unified-trading-api, fund-administration-service, greeks-service,
# ibkr-gateway-infra, batch-live-reconciliation-service, alerting-service, client-reporting-api,
# deployment-api, deployment-service) are candidates for the identical treatment once each is flipped
# private — not yet done, do not add them here until their own visibility flip is confirmed first.
#
# trading-agent-service + deployment-api + deployment-service + unified-trading-library RE-ADDED
# 2026-08-14 (same operator-directed sweep, same session, later same day): same order (private flip
# verified first, then dormant pool re-installed + confirmed `online` via GitHub API), same
# GLUE_COUNT=1/WRITER_COUNT=0 shape. Host load checked before/after each install (peaked ~load avg
# 0.14-0.7 across the whole 4-repo batch, no contention) — unified-trading-library specifically
# carries prior host-thrashing revert history (2026-07-28, see above) so its install was done last,
# after confirming the other three landed clean. Remaining public repos with a dormant pool
# (system-integration-tests, unified-trading-api, fund-administration-service, greeks-service,
# ibkr-gateway-infra, batch-live-reconciliation-service, alerting-service, client-reporting-api) —
# operator explicitly held back fund-administration-service and batch-live-reconciliation-service
# from this sweep; the rest are simply not yet actioned. Same rule: do not add any of them here until
# its own visibility flip is confirmed first.
#
# Remaining 14 are PRIVATE and genuinely need self-hosting for billing:
agent-orchestrator
strategy-service
e2e-testing
features-service
market-tick-data-service
execution-service
ml-service
instruments-service
unified-api-contracts
market-data-processing-service
trading-agent-service
deployment-api
deployment-service
unified-trading-library
