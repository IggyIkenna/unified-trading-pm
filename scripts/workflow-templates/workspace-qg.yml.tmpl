# workspace-qg.yml — Managed by rollout-workflow-templates.sh
# Template SSOT: unified-trading-pm/scripts/workflow-templates/workspace-qg.yml.tmpl
# DO NOT edit this file directly. Edit the SSOT template and re-run rollout.
#
# Calls the unified python-quality-gates reusable workflow. dep_repos is
# derived from workspace-manifest.json at rollout time and pinned here.

name: workspace-qg

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-gates:
    uses: IggyIkenna/unified-trading-pm/.github/workflows/python-quality-gates.yml@live-defi-rollout
    with:
      dep_repos: "{{DEP_REPOS}}"
    secrets: inherit
