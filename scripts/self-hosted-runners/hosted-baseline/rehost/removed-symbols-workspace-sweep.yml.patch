--- derived
+++ hosted
@@ -59,19 +59,13 @@
           token: ${{ secrets.GH_PAT }}
           path: workspace/unified-trading-pm

-      # No actions/setup-python: this runs on the self-hosted glue pool, whose python3 IS the slot
-      # venv on 3.13 (first on the unit PATH) — the version pyproject requires. Re-adding it would
-      # re-download a Python that is already resident, on a box we pay for 24/7. Provisioning lives
-      # in scripts/self-hosted-runners/bootstrap-ci-host.sh; if the version is ever wrong, fix it
-      # THERE, not here. The step this replaced pinned "3.12", which CONTRADICTED the workspace's
-      # requires-python = ">=3.13,<3.14" — the pin was never load-bearing, just stale.
-      # NOTE: this workflow is now self-hosted-ONLY — restoring runs-on: ubuntu-latest requires
-      # restoring setup-python with it.
+      - name: Set up Python
+        uses: actions/setup-python@v6
+        with:
+          python-version: "3.12"

-      # No "Install checker dependency (PyYAML)" step: this job's python3 IS the runner's slot venv,
-      # which already provides pyyaml (scripts/self-hosted-runners/slot-venv-requirements.txt).
-      # Self-hosted-ONLY: restoring runs-on: ubuntu-latest requires restoring this step too
-      # (see scripts/self-hosted-runners/hosted-baseline/removed-symbols-workspace-sweep.yml).
+      - name: Install checker dependency (PyYAML)
+        run: pip install pyyaml

       - name: Clone all sibling repositories (shallow)
         env:
