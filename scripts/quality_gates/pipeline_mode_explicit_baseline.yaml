# Baseline for check_pipeline_mode_explicit_at_record_calls.py
#
# Each entry = a CURRENTLY-KNOWN occurrence of a `record_*` call without an
# explicit `pipeline_mode=` kwarg. Surfaces as WARNING (informational, exit-clean).
# A new occurrence not in this file → ERROR, exit 1.
#
# Bootstrap baseline written 2026-05-12 by slot 8 from the full workspace sweep.
# Per-repo split (current state after 2026-05-12 Phase 4.FEATURES sweep —
# harsh slot 3 cleared the 6 features-service entries):
#   market-tick-data-service: 0 entries (Phase 4.MTDS sweep landed 2026-05-12
#     at MTDS@3da3f43 — 97 callsites in 23 files + DefiManifestRecorder
#     Q1=(α) migration; UTL@12d5e62 11 internal callsites; UAC@52d289c
#     PipelineMode enum extension + 14 DeFi SOURCE_PRIORITY entries.)
#   features-service: 0 entries (Phase 4.FEATURES sweep landed 2026-05-12 by
#     harsh slot 3 — sports/batch_handler 4 callsites + calendar_orchestrator
#     2 callsites cleared. Calendar uses BATCH_INSTRUMENTS_SERVICE workaround
#     per features_calendar_pipeline_mode_gap_2026_05_12.md; clean fix
#     (BATCH_FRED + BATCH_FEATURES_CALENDAR_SERVICE) is an operator-decision
#     UAC enum extension pending.)
#   unified-trading-library: 0 entries (Phase 4.UTL sweep landed 2026-05-12 —
#     11 callsites cleared: streaming/candle_writer 4 LIVE_WEBSOCKET stamps,
#     streaming/parallel_per_symbol_runner caller-passed pipeline_mode kwarg,
#     streaming/live_aggregator Protocol-delegating whitelist marker,
#     manifest_writer_normalising 3 wrappers extended with required PipelineMode,
#     per_leaf_failure router pipeline_mode field threaded to record_failed,
#     manifest_writer:1919 _validate_with_source plumb-through from
#     _maybe_validate/record_captured.)
#
# Status closed set (no entries today, kept for future Phase-N sweeps):
#   - pending_phase_4_mtds      → Phase 4.MTDS Q1-Q5 triage gate (slot 3)
#   - pending_phase_4_features  → features-consolidation merge gate (~2026-05-16)
#
# Clear cadence: entries are DELETED from this file (not re-statused) as Phase 4.MTDS
# sweep + features-consolidation Phase 4.FEATURES sweep + DEFAULT-REMOVAL land.

default_successor: "manifest_schema_final_gate Phase 4.DEFAULT-REMOVAL"

entries: []
