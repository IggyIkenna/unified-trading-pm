# Baseline for check_canonical_model_regressions.py (STEP 5.93) — pre-existing
# canonical-model occurrences tolerated as WARNINGS (exit-clean). NEW ones fail.
# SSOT: audit_criteria_automation_2026_06_08.md (Tier-2).
#
# The `era-a-chain-write` pattern was REMOVED 2026-06-09 (operator clarification):
# `options_chain`/`futures_chain` are a NAME COLLISION — each is both an
# INSTRUMENT_TYPE (the per-underlying chain bundle, trades = data_type=trades)
# AND a genuine DATA_TYPE (the IV/greeks/mark chain SNAPSHOT,
# CEFI/TRADFI_OPTIONS_CHAIN_SNAPSHOT; matrix ("tradfi","options_chain") →
# {trades, ohlcv_1m, options_chain}). So `data_type="options_chain"` is a
# LEGITIMATE Era-B snapshot write; a static literal check can't tell flat-Era-A
# from nested-Era-B. The 3 formerly-baselined sites (tardis_adapter +
# MDPS options/futures_chain adapters) are the LEGIT snapshot writers, not
# violations. Era-A (flat vs nested) is enforced at RUNTIME by _LEGAL_DATA_TYPES
# + bundle-grain, which has the instrument_type context this static gate lacks.
default_successor: n/a
entries: []
