# Node/UI repo .gitignore template
# Reference: instruments-service, unified-trading-library

# Security - Never commit credentials
.env
.env.local
.env.*.local
.DS_Store
*.backup

# Dependencies
# No trailing slash: per-slot worktrees symlink node_modules to a shared install, and
# "node_modules/" matches directories only → the symlink stays un-ignored (slot reads dirty).
node_modules

# Build outputs
.next/
dist/
build/
.turbo/
out/
*.tsbuildinfo

# Coverage
.nyc_output/
coverage/
