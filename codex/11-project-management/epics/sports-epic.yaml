# ==============================================================================
# Sports Betting Epic — Readiness Definition
# ==============================================================================
#
# MVP Priority: 4 (structure defined; completion not enforced for DeFi MVP)
# Business invariant: PnL confirmed via backtest (BR5) for all service repos.
# Dependency: CAN develop in parallel with CeFi/TradFi (does NOT require CeFi complete).
#
# Sports strategy in scope (from mvp-universe.yaml):
#   - arb (arbitrage across sportsbooks)
#
# Sports venues in scope (MVP):
#   - BETFAIR (CLOB via unified-sports-execution-interface)
#   - POLYMARKET (prediction markets)
#
# See: unified-trading-pm/codex/11-project-management/mvp-universe.yaml
# ==============================================================================

epic_id: sports
display_name: "Sports Betting Epic"
mvp_priority: 4
business_requirement_minimum: br5_pnl_backtest
last_updated: "2026-03-11"
dependency_note: "Sports epic develops in parallel; does NOT require CeFi epic complete."

completion_criteria:
  code: cr5_quickmerged_main
  deploy: dr3_smoke_green
  business: br5_pnl_backtest

required_repos:
  # ── Core ─────────────────────────────────────────────────────────────────

  - repo: unified-api-contracts
    arch_tier: library-t0
    min_stage: cr5
    min_br_stage: na
    asset_group: core

  - repo: unified-cloud-interface
    arch_tier: library-t0
    min_stage: cr5
    min_br_stage: na
    asset_group: core

  - repo: unified-trading-library
    arch_tier: library-t0
    min_stage: cr5
    min_br_stage: na
    asset_group: core

  - repo: unified-config-interface
    arch_tier: library-t0
    min_stage: cr5
    min_br_stage: na
    asset_group: core

  - repo: unified-trading-library
    arch_tier: library-t1
    min_stage: cr5
    min_br_stage: na
    asset_group: core

  - repo: unified-domain-client
    arch_tier: library-t3
    min_stage: cr5
    min_br_stage: na
    asset_group: core

  - repo: instruments-service
    arch_tier: service
    min_stage: cr5
    min_br_stage: br5
    asset_group: core

  - repo: strategy-service
    arch_tier: service
    min_stage: cr5
    min_br_stage: br5
    asset_group: core

  - repo: execution-service
    arch_tier: service
    min_stage: cr5
    min_br_stage: br5
    asset_group: core

  - repo: pnl-attribution-service
    arch_tier: service
    min_stage: cr5
    min_br_stage: br5
    asset_group: core

  # ── Sports-specific ───────────────────────────────────────────────────────
  # unified-sports-execution-interface entry removed 2026-07-24 (operator ruling, workspace-wide epic-yaml scrub):
  # USEI was merged into execution-service 2026-03-26 and no longer exists as a standalone repo; its function is
  # already covered by the execution-service entry above. The old entry was a permanently-unsatisfiable required gate.

  - repo: features-service (sports family)
    arch_tier: service
    min_stage: cr5
    min_br_stage: br5
    asset_group: sports
    rationale: "Pre-match ML features (team form, H2H, venue, weather, market odds)."

optional_repos:
  - repo: features-service (delta-one family)
    asset_group: cefi
    note: "CeFi feature service. Not required for sports arb strategy."

  - repo: ml-training-service
    asset_group: cefi
    note: "ML models optional for sports MVP (arb is rule-based)."

  - repo: ml-inference-service
    asset_group: cefi
    note: "Same as above."

  # unified-defi-execution-interface entry removed 2026-07-24 (operator ruling, workspace-wide epic-yaml scrub):
  # UDEI was merged into execution-service 2026-03-26 and no longer exists as a standalone repo.

  - repo: features-service (onchain family)
    asset_group: defi
