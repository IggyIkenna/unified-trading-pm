---
description: "Service→UDS→UCS domain boundary, instruments domain reader, and API key resolution"
alwaysApply: true
priority: 70
tags: [instruments, api-keys, data-access, uds, domain-layer]
---

# Domain Data Layer: Service → UDS → UCS

RULE: Services use unified_domain_client for ALL domain data. unified_trading_services is for cloud primitives only.
WHY: UTS domain client stubs have been deleted — importing them from UTS will fail at import time.
CODEX: 02-data/subscription-model.md, 05-infrastructure/unified-libraries/LIBRARY-DEPENDENCY-MATRIX.md
ENFORCED BY: UCS_DOMAIN_IMPORT quality gate check (blocking)

## Import from UDC (domain layer):
```python
from unified_domain_client import (
    InstrumentsDomainClient,         # reading instruments
    DomainValidationService,         # domain validation service
    DomainValidationConfig,          # domain validation config
    validate_timestamp_date_alignment,
    create_instruments_client,       # factory
    determine_market_category,       # market category classification
    filter_instruments_by_category,  # filter instruments DF by category
)
```

## Import from UCI (cloud I/O — preferred for all tiers):
```python
from unified_cloud_interface import (
    get_storage_client, get_secret_client, get_data_sink, get_data_source,
)

# Writing domain data to cloud storage:
sink = get_data_sink(routing_key="instruments")  # maps to PROTOCOL_DATA_SINK_BUCKET_INSTRUMENTS
sink.write(data=output_df, path=output_path)
```

## Import from UTS (cloud primitives — backward compat only):
```python
from unified_trading_services import (
    get_storage_client, get_secret_client,  # re-exported from UCI
    handle_api_errors, BaseGCSWriter, ColumnSchema, generate_date_range,
)
```

**Note:** `CloudTarget` and `StandardizedDomainCloudService` are **fully deleted** from UTS/UDC. Use `get_data_sink(routing_key=...)` from `unified_cloud_interface` for all domain data writes. Direct `unified_cloud_interface` imports are preferred over UTS re-exports for T2+ libs.

## API Keys: Secret Manager Only
```python
api_key = get_secret_client().get_secret(secret_name="<secret-name>")
```

Key API key secret names (from `07-security/secrets-management.md`):
- `tardis-api-key` — Tardis.dev historical tick data
- `databento-api-key` — Databento market data
- `coinglass-api-key` — Coinglass liquidation heatmap / open interest
- `thegraph-api-key` — The Graph subgraph queries
- `alchemy-api-key` — Alchemy node RPC (Ethereum, DeFi)

NEVER:
- `get_secret_client(..., fallback_env_var=...)` — silently swallows missing-secret errors
- `CloudTarget` — **DELETED** from UTS; use `get_data_sink(routing_key=...)`
- `StandardizedDomainCloudService` — **DELETED** from UDC; use `get_data_sink(routing_key=...)`
- `from unified_trading_services import InstrumentsDomainClient` (deleted stub)
- `from unified_trading_services import create_instruments_client` (deleted stub)
- `from unified_trading_services import determine_market_category` (moved to UDC)
- `from unified_trading_services import DomainValidationConfig` (re-exported from UDC)
- `from unified_trading_services import DomainValidationService` (re-exported from UDC)
- `os.environ.get("TARDIS_API_KEY")` or any `os.environ` for secrets — use Secret Manager
- Direct GCS reads for instruments; duplicate `_load_instruments_by_venue` in services
