---
description: "Service domain data schemas (BaseModel/TypedDict/dataclass contracts) belong in unified-internal-contracts domain/<service>/, not in service repos"
alwaysApply: true
priority: 80
tags: [schema, contracts, uic, domain]
---

# Service Domain Schema Placement

## RULE

Every domain data schema (Pydantic BaseModel, TypedDict, @dataclass used as a data contract) that describes a
service's primary output belongs in `unified-internal-contracts/unified_internal_contracts/domain/<service-name>/`.

Services NEVER define domain schemas locally. Services import from UIC via unified-trading-library or
unified-domain-client.

WHY: If schemas are defined in service repos, other services cannot discover them without importing the service
(tier DAG violation). Centralizing in UIC makes all data shapes discoverable from one place with no
cross-service imports.

CODEX: 02-data/contracts-scope-and-layout.md, 02-data/schema-governance.md
AUDIT: plans/active/SCHEMA_CONTRACTS_AUDIT.md

## WHAT GOES IN UIC domain/<service-name>/

- The Pydantic/TypedDict/dataclass that represents the data rows a service writes or publishes
- Feature records, position records, execution records, instrument definitions, etc.
- Anything another service or reader needs to know the shape of this service's data

## WHAT STAYS IN THE SERVICE

- `SchemaDefinition` / `ColumnSchema` objects (parquet write enforcement, from unified-trading-library) → stay in `schemas/output_schemas.py`
- HTTP request/response models used only in that service's API layer (FastAPI DTOs)
- Internal helper classes not shared across repos

## WHAT GOES IN UIC ELSEWHERE (not domain/)

- Event envelopes → UIC `events.py` / `pubsub.py`
- Shared identifiers cross-imported by 2+ repos → UIC `reference/`
- Canonical market data types → UIC `market_data/`
- Error records → UIC `schemas/errors.py`

## DO:

  # 1. Define in UIC
  # unified-internal-contracts/unified_internal_contracts/domain/my-service/output.py
  class MyServiceRecord(BaseModel):
      timestamp: datetime
      instrument_key: str
      value: Decimal

  # 2. Import in service
  from unified_internal_contracts.domain.my_service.output import MyServiceRecord

  # 3. SchemaDefinition stays in service (infrastructure only)
  # schemas/output_schemas.py
  MY_SCHEMA = SchemaDefinition(
      name="my_output", version="1.0",
      columns=[ColumnSchema(name="timestamp", dtype="datetime64[ns, UTC]"), ...]
  )

## NEVER:

  # BAD: domain schema defined in service
  class MyFeatureRecord(BaseModel):  # ← in my_service/models.py
      instrument_key: str
      value: float

  # BAD: another service importing from service (tier violation)
  from my_service.models import MyFeatureRecord

  # BAD: putting a cross-repo TypedDict in a service
  class InstrumentRow(TypedDict):  # ← belongs in UIC reference/
      instrument_key: str
      venue: str

## KNOWN VIOLATIONS (from 2026-03-05 audit)

See plans/active/SCHEMA_CONTRACTS_AUDIT.md Section 3b:
- execution-service/sports/models.py → domain/execution-service/sports.py
- strategy-service/models/domain_data.py + order.py → domain/strategy-service/
- market-data-processing-service/models.py + base_adapter.py → domain/market-data-processing/
- market-data-api/core/orderbook_subscriber.py → domain/market-data-api/
- features-onchain-service/models.py → UIC features.py (OnchainFeatureRecord)
- features-sports-service/schemas/output_schemas.py (column lists) → UIC sports.py (TypedDicts)
- unified-domain-client/schemas/instrument_key.py → UIC reference/ (InstrumentKey)
