---
description: "Use aiohttp for async HTTP — never requests in async functions"
alwaysApply: true
priority: 80
tags: [async, http, aiohttp]
---

# Async HTTP: Use aiohttp, Not requests

RULE: Use `aiohttp` for all HTTP calls in async code. Never use `requests` inside async functions.
WHY: requests is synchronous and blocks the event loop; aiohttp is non-blocking and designed for async.
CODEX: 06-coding-standards/README.md

DO:
  import aiohttp
  async def fetch(url: str) -> dict[str, object]:
      async with aiohttp.ClientSession() as session:
          async with session.get(url) as response:
              return await response.json()

NEVER:
  requests.get(url)  # blocks event loop in async context
  asyncio.run() inside running event loop
  time.sleep() in async functions (use await asyncio.sleep())
  Creating new ClientSession per request (reuse sessions)
