---
doc_type: cursor-rule
description: Always use quickmerge.sh, never run quality gates standalone
alwaysApply: true
priority: 90
tags:
- ci-cd
- quickmerge
---
# Always Use Quickmerge

RULE: ALWAYS run `bash scripts/quickmerge.sh "message"`. NEVER run `scripts/quality-gates.sh` standalone. Use `--dep-branch` when dependencies differ from main. Branch protection rejects direct push to main.
WHY: Quickmerge runs dependency cascade, pre-flight audit, quality gates, Act simulation, and PR creation. Standalone quality gates miss critical checks.
CODEX: 06-coding-standards/feature-branch-workflow.md, 06-coding-standards/quality-gates.md

## LDR Trunk Model (current — updated 2026-08-21, replaces the retired three-tier model)

```
feat/*              → QG only, no PR (feature iteration — auto-detected, no --no-pr needed)
live-defi-rollout    → quickmerge lands every commit here; LDR is the trunk SSOT
main                 → default promote is LDR→main DIRECT (staging DORMANT); auto-promoted via
                        ldr-to-main-promote-fleet.yml + ldr-to-main-promote.yml (*/30, auto-merge)
staging              → KEPT + REVERSIBLE, not the default path; a major/breaking bump or an
                        operator decision routes THROUGH it instead of straight to main
```

| Commit type | Target | Command |
|-------------|--------|---------|
| `fix:` / `feat:` / `chore:` | LDR → main (auto-promote) | `bash scripts/quickmerge.sh "fix: ..."` |
| `feat!:` / `BREAKING CHANGE:` / major bump | LDR → staging → main | operator-decided; do not rely on `--to-staging` (see below) |
| Feature iteration | no PR | auto when on `feat/*` branch |

**`--to-staging` is a no-op today** (kept for backwards compat — the flag does not route anywhere). Do not use it to
route a breaking change; a major/breaking bump is an operator-decided path through staging, not a quickmerge flag.
**Hotfix path:** `--hotfix` routes to staging with abbreviated SIT (<2 min, production incidents only); `--hotfix-to-main`
is the operator-gated break-glass fast-track straight to `main`.
**SSOT:** `unified-trading-pm/cursor-configs/CLAUDE.md` § "Git discipline + shipping pipeline",
`codex/08-workflows/ci-cd-flow.md` — read these for the current gate set rather than trusting this file's own prior
snapshot, which had drifted stale.

## Dependency Cascade

- **Feature mode (--dep-branch):** cascades across dep repos on a named feature branch; QG only, no PR. Branch auto-derived from current git branch.
- **`--to-staging` is a no-op today** (see "LDR Trunk Model" above) — it does NOT cascade a breaking change through dependents. The real breaking-change cascade is PM-driven: the CI version-bump dispatches to PM, which dispatches a `dependency-update` to each direct dependent per the actual dependency graph (`workspace-manifest.json`) — see `codex/08-workflows/dependency-cascade.md`.
- **`--branch NAME` + `--to-staging`** → hard fail (mutually exclusive) — flag-parsing validation only; the flag itself performs no routing.
- The cascade is **selective**: only repos with a dependency edge to the changed repo are touched. Not all 60 repos.

**SSOT:** `unified-trading-pm/codex/08-workflows/ci-cd-flow.md` (§ "Dependency promotion") + `codex/08-workflows/dependency-cascade.md`

## Agent Sessions — MANDATORY

RULE: Agents (Claude Code, CI agents, run-agent.sh) MUST pass `--agent` to quickmerge.

Two-pass model:
- **Pass 1** — full QG: `bash scripts/quality-gates.sh` or `run-all-quality-gates.sh` — tests, typecheck, lint, codex all run
- **Pass 2** — quickmerge: lightweight verify only — lint + format + typecheck + codex; tests already passed in pass 1

`--agent` = skip act + skip tests. Quickmerge becomes a fast format/lint gate before PR creation.

## Push before quickmerge (local commits)

Quickmerge **stashes uncommitted** changes before switching branches, then **stash pop** — that protects dirty trees, not
**commits that exist only locally**. Stage 5 may run `git checkout -B <branch> origin/<branch>`, which sets the branch tip
to match the remote; **unpushed commits** can stop being reachable from the branch name (still in reflog until GC).

**Do this:** If you (or a prior step) already ran `git commit` on `feat/your-branch` and did **not** push, run
`git push -u origin feat/your-branch` **before** quickmerge. **Or** use quickmerge as the primary commit path after Pass 1
QG so there are no orphan local-only commits.

**Agents:** Same rule — never run quickmerge on a branch that is ahead of `origin` with unpushed commits unless you push
first (or you intend only uncommitted work + quickmerge’s own commit).

```bash
bash scripts/quickmerge.sh "feat: ..." --agent                          # skip act + tests
bash scripts/quickmerge.sh "feat: ..." --agent --skip-typecheck         # also skip typecheck
bash scripts/quickmerge.sh "feat: ..." --agent --skip-tests             # redundant (--agent already skips tests)
```

User can harden by omitting `--agent` (full QG re-runs including tests + act). Use `--quick` for human shortcut (skips act only, tests still run).

## Key Flags

`--to-staging` (breaking change cascade) | `--no-pr` (QG only, auto on feat/*) | `--dep-branch "feat/X"` (feature cascade) | `--unit-only` | `--agent` (agent sessions — skip act) | `--quick` (human shortcut — skip act) | `--skip-tests` | `--files "a.py b.py"`

Full flags: CODEX 06-coding-standards/quality-gates.md

## DO

```bash
bash scripts/quickmerge.sh "feat: add ConfigReloader"
bash scripts/quickmerge.sh "fix: tier boundary" --dep-branch "feat/rollback"
bash scripts/quickmerge.sh "chore: cleanup" --no-pr
```

## NEVER

```bash
bash scripts/quality-gates.sh                   # standalone — misses dep validation + PR
git add -A && git commit && git push            # bypasses all quality gates
git reset --hard origin/main                    # FORBIDDEN — destroys local changes
Finish with uncommitted changes in any modified repo    # commit your OWN edits — see note below
```

"Finish with uncommitted changes" targets the agent's **own** edits — never abandon your work in a dirty tree. It does
**NOT** mean sweeping another agent's concurrent WIP into your `--files` list. If dep-repo working trees you don't own
are dirty, **STOP and report to the user** before quickmerge; do not auto-bundle them. See CLAUDE.md key-rules and
`SUB_AGENT_MANDATORY_RULES.md` (Quickmerge Safety) — "DO NOT autonomously run quickmerge when local dep repos are dirty".

## Nothing-to-Commit on Staging

`git commit --allow-empty -m "chore: promote feat/X to staging"` then `bash scripts/quickmerge.sh "chore: promote feat/X to staging"`
