# live-health-monitor-ui: Per-Repo Rules

## Service-Specific Context

**Type**: ui
**Epic**: See `unified-trading-codex/11-project-management/epics/`
**Template**: `unified-trading-codex/10-audit/_service-ui.yaml`

## Workspace Rules

Inherits all rules from workspace `.cursorrules`: quality gates, quickmerge (with Cursor Team Kit for CI/reviews), config standards, cloud-agnostic design, event logging.

**⚠️ RUNTIME VERIFICATION (P0 BLOCKING GATE):** NEVER claim "done" without actual runtime verification. For UI: must run `npm run dev`, wait 8-10s, read terminal output, grep for errors, verify NO errors present. See `.cursor/rules/runtime-verification-required.mdc`.


**UI-Specific Cursor Team Kit**: Use `/deslop` before committing, `/run-smoke-tests` for Playwright validation. **Agent automatically starts ci-watcher after quickmerge** (no user prompt needed).

**Smoke Tests**: Playwright smoke tests in `tests/smoke/` (7 tests, ~1 min runtime). Run `npm run smoketest`. See `unified-trading-codex/14-testing-guides/UI_SMOKE_TESTING_GUIDE.md`.

## Service-Specific Patterns

[Add service-specific patterns here]

## Architecture Notes

[Add architecture notes here]

## Dependencies

[List key dependencies here]

---

## SSOT Alignment Validation

See: .cursor/rules/workflow/ssot-alignment-enforcement.mdc

## External Import Standards

See: .cursor/rules/core/external-import-standards.mdc
