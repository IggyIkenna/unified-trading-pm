---
doc_type: plan
title: >-
  DeFi distinct-values panel — drive every axis (venues/chains/instrument_types/data_types) to zero non-canonical, not a
  reduced count — in-flight tracking for a 9-way parallel dispatch
summary: >-
  Operator corrected this session's initial posture (treating several findings as "low priority, scope only, defer") —
  the actual bar is ZERO non-canonical values across every axis, permanently, with live writers and backfills verified
  not to regress the fix, not a partial cleanup. This doc is the durable tracking record for the resulting 9-way
  parallel dispatch (7 sub-agents + 2 directly-executed scripts) so results can be reconciled even if the dispatching
  session's context is lost. Checkpoint written under context pressure (68% usage) per this workspace's pre-compact
  ritual — treat every "in progress" item below as needing a live status check (git log / manifest read), not as
  evidence anything actually landed.
status: active
nature: process
asset_group:
  [defi] # corrected 2026-08-06 (ag-closeout-audit cross-cutting orthogonality fix) -- was [defi, cross-cutting], a
  # genuine mistag: every substantive item (gas_fees legacy-chain venues, POOL casing, dex_pools/dex_swaps/
  # rate_indices, HYPERLIQUID/EXTENDED/LIGHTER/BLAZESTAKE/GMX/KAMINO_LENDING venues) is defi-manifest-specific, not a
  # mechanism spanning multiple asset groups. cross-cutting dropped.
stage: [data]
repos:
  [
    unified-trading-pm,
    unified-api-contracts,
    market-tick-data-service,
    strategy-service,
    features-service,
    instruments-service,
    unified-trading-library,
  ]
scope: [engineer, admin]
tags:
  [defi, distinct-values, canonicalisation, manifest, venues, chains, instrument-types, data-types, dispatch-tracking]
related:
  [
    /plans/active/issues/defi_cefi_venue_chain_axis_contamination_2026_07_28.md,
    /plans/archive/issues/defi_legacy_precanonical_composite_venue_objects_2026_07_24.md,
    /plans/active/issues/defi_legacy_data_type_names_manifest_migration_scope_2026_08_04.md,
    /plans/archive/issues/defi_dex_pool_fees_retirement_recommendation_2026_08_04.md,
    /plans/archive/2026_08/issues/defi_hyperliquid_residual_manifest_rows_2026_08_04.md,
    /plans/archive/2026_07/defi_gmx_venue_removal_2026_07_25.md,
    /plans/archive/issues/defi_perp_daily_ctx_manifest_gap_reader_risk_2026_07_22.md,
    /codex/02-data/gcs-and-manifest-delete-safety-protocol.md,
  ]
created: "2026-08-04"
last_updated: "2026-08-07"
parent_epic: manifest_master
assigned_vm: NA
execution_scope: local-only
priority: P1
estimate_class: refactor
estimate_baseline_ai_days: 3
estimate_calibrated_ai_days: 3.6
source: >-
  Interactive session 2026-08-04, operator corrected the dispatching session's "defer, don't execute" posture twice;
  this dispatch is the direct response — verify then EXECUTE every finding, not just document it.
assigned_role: data_engineering
drift_direction: advance-code
locked_by:
locked_since:
supersedes:
superseded_by:
resolved_by:
depends_on: []
context_scope:
  [
    /plans/active/issues/defi_manifest_allow_stale_fallback_incomplete_for_long_pause_2026_08_07.md,
    /plans/active/issues/defi_gas_fees_legacy_purge_manifest_step_blocked_vm_infra_flakiness_2026_08_05.md,
    /plans/active/issues/defi_legacy_data_type_names_manifest_migration_scope_2026_08_04.md,
    /plans/active/defi_track01_per_instrument_and_canon_id_2026_07_24.md,
    /codex/02-data/reconciliation-finding-taxonomy.md,
    /codex/02-data/canonical-cutover-register.md,
    /codex/02-data/non-canonical-path-inventory.md,
    /codex/02-data/defi-canonical-naming-ssot.md,
  ]
---

# DeFi distinct-values — zero non-canonical dispatch (2026-08-04)

## Full non-canonical inventory the operator pasted (live panel read, this session — treat as the ground truth to

re-verify against, not the earlier 16-20-count estimates in older docs)

**Venues (~42 non-canonical)**: 9 chain-name venues (ARBITRUM/AURORA/AVALANCHE/BASE/BSC/ETHEREUM/LINEA/OPTIMISM/ POLYGON
— gas_fees legacy), 5 CeFi venues (BITFINEX/BITGET/BYBIT/KRAKEN/OKX), 4 on-chain-CLOB-reclassified venues
(BLAZESTAKE/EXTENDED/HYPERLIQUID/LIGHTER), GMX, KAMINO_LENDING, and **22 legacy `PROTOCOL-CHAIN` composite venues**
(AERODROME_V3-BASE, BALANCER×6, CAMELOT_V3-ARBITRUM, CURVE×2, PANCAKESWAP_V3×3, SUSHISWAP+SUSHISWAP_V3×4, UNISWAP_V3×5).
**Chains (2 non-canonical)**: FUTURES (fixed, code already shipped), HYPERLIQUID. **Instrument types (1 non-canonical by
scale, not by UI badge)**: `POOL` (uppercase) vs `pool` (lowercase) — silenced from the UI badge by a pre-existing
case-insensitive comparison exception, but **measured this session at 1,919,789 real manifest rows**
(`data_type=dex_pool_swaps` 1,904,256 + `dex_swaps` 15,533; `capture_status=captured` 1,919,766), spread across every
major DEX protocol. This is NOT the "small P3 cleanup" the dispatching session first assessed it as — correcting that
here explicitly. **Data types (6 non-canonical)**: `dex_pool_fees`, `dex_pools`, `dex_swaps`, `rate_indices`,
`perp_daily_ctx` (fix shipped this session, panel just hasn't refreshed — nightly rollup, cached from 2026-08-02),
`perp_mark_price`.

## In-flight work (9-way parallel dispatch) — CHECK LIVE STATUS, do not trust this table's state past the moment it

was written

| #   | Scope                                                                                                                                                                                                                                        | Mechanism                                                                                                                                | Status at checkpoint time                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | How to check current status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | gas_fees legacy-prefix delete (10 venues, 6,080 objects — real script is `purge_gas_fees_legacy_venue_prefixes_2026_08_04.py`, the row-1 script name below was never actually committed, likely stale/abandoned WIP from an earlier session) | Directly executed by dispatching session                                                                                                 | **GCS-object delete 100% COMPLETE** (verified twice, directly, via a 10-venue-wide `match_glob` check — 0 remaining). **Manifest purge (12,425 rows) NOT complete** — blocked by a currently-active VM-boot `gsutil` infra hang, not a script bug; 3 IAM gaps + 2 script reliability issues found/fixed/shipped along the way. Consolidator cron resumed (was never mutated).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | See `/plans/active/issues/defi_gas_fees_legacy_purge_manifest_step_blocked_vm_infra_flakiness_2026_08_05.md` for full detail + next-step todos.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2   | POOL→pool casing fold (1,919,789 rows)                                                                                                                                                                                                       | Interactive session 2026-08-05                                                                                                           | **DONE.** Classified all 1,941,088 legacy `POOL` captured rows into twin-exists (193 distinct cells — a canonical `pool`-cased row already existed) vs no-twin (277,366 distinct cells — real, non-duplicate data with no canonical twin) via `fold_pool_instrument_type_casing_2026_08_05.py`. Verified the no-twin population safe to case-fix by direct GCS path checks across 3 venues (AERODROME_V3/BASE, UNISWAP_V3/ETHEREUM, SUSHISWAP_V3/BASE) — the underlying objects were already correctly at lowercase `instrument_type=pool` paths, so `POOL` was a pure historical manifest-recording bug, decoupled from the actual write path. Ran a single combined `--apply --fold-no-twin-cells` VM pass against prod: **1,351 rows retired** (twin-exists, capture_status captured→attempted_failed) + **1,937,619 rows folded** (no-twin, instrument_type POOL→pool case-fixed in place). Verified directly against the freshly-written index: **0 remaining `captured` rows with instrument_type=POOL** for `dex_pool_swaps`/`dex_swaps` — full migration to canonical, nothing left labeled `POOL`. One bug found+fixed en route: the script's Pass 2 computed a cell-key for every row in a row group (not just legacy candidates) before masking, so a non-legacy row's null `instrument_id` crashed the whole run via `NoneType.upper()` — confirmed the crash happened before any snapshot/upload (safe failure, 0 rows touched), fixed to only key legacy candidates (`market-tick-data-service@87e9e100`). Launcher also needed 2 fixes: allow-list the literal `--fold-no-twin-cells` value (deployment-service `launch-canonical-migration-vm.sh`) + bump `MAX_DURATION` 300s→600s (the repo's actual test-suite runtime has grown to 350-560s, unrelated pre-existing drift, not caused by this change) — both `deployment-service@248df0d8`. Manifest consolidator cron (paused for this + item 4's dex_pools work) resumed after this landed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Verify directly: a fresh distinct-values census on `instrument_type` for `dex_pool_swaps`/`dex_swaps` shows no `POOL` (uppercase) among captured rows.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 3   | Fold 22 composite `PROTOCOL-CHAIN` venues                                                                                                                                                                                                    | Interactive session 2026-08-05                                                                                                           | **RESOLVED — false alarm, not a bug.** Root-caused via a VM-run row-level provenance trace (`market-tick-data-service/scripts/one_offs/trace_composite_venue_provenance_2026_08_05.py`): writer is `market-data-processing-service`'s already-completed one-time `backfill_defi_dex_pool_swaps_source_correction.py` campaign (2026-08-03/04, 813,150 objects copied, 0 errors). Real backing data confirmed under MDPS's own `processed_candles/` path (the 2026-08-04 investigation only checked MTDS's `raw_tick_data/` convention — wrong path, not wrong conclusion). No fold, no purge, nothing to execute.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | See `plans/active/issues/defi_underscored_multichain_composite_venue_fold_2026_08_04.md` (status: resolved) for full detail.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 4   | Migrate `dex_pools`/`dex_swaps`/`rate_indices` (~4M rows)                                                                                                                                                                                    | Interactive session 2026-08-05 (dex_pools); fold script iterated + run against prod 2026-08-07, `dex_swaps` STILL not genuinely complete | **`dex_pools` DONE** (unchanged, see prior entry). **`dex_swaps`/`rate_indices` — genuinely NOT complete, 2 real bugs found+fixed+shipped in the fold script this session, 1 VM run explicitly disowned as invalid.** Found & fixed: (1) `InstrumentType(shard.instrument_type)` crashed on lowercase manifest values — fixed to `.upper()` per the workspace's own C2a case-insensitive ruling (`market-tick-data-service@513a83dd`); (2) a genuine `ManifestWriter.add()` thread-safety race — concurrent shard workers could interleave/duplicate rows because `_write_lock` was declared but never acquired — fixed at script level with a caller-side `threading.Lock()` around every `recorder.add()` call, validated via an isolated stress test before shipping (`market-tick-data-service@94e625c7`, bundled with a QG `MAX_DURATION` 600→800 fix hit while validating). A subsequent VM run (`--only dex_swaps`) reached ~85%+ shard progress before an OOM kill — real, substantial progress, not yet complete. A follow-up attempt used a newly-added `--allow-stale-fallback` launcher flag (`deployment-service@cb6397815`) to work around the long-paused DeFi manifest consolidator — this was a MISTAKE: the flag is memory-safe (row-group pushdown) but NOT data-complete when the consolidator has been paused longer than a prune cycle, since per-VM shards get pruned after each consolidation and the fallback reader only sees currently-existing shards, not the stale consolidated blob. Caught it myself via a suspiciously small result (**260 shards captured vs the expected ~27,549 — only ~1.4% real coverage** while reporting clean success), root-caused it by reading `_read_and_merge_per_vm_shards`/`manifest_consolidator.py` directly, corrected the launcher's comment (`deployment-service@3dd83fef8`), and filed a full issue doc marking that specific run as **NOT a valid completion**. Currently waiting (no fallback flag, safely) for another session's `canonical-migration-defi-rebuild` VM to finish so the consolidator genuinely catches up before a clean on-demand relaunch. `rate_indices` **GENUINELY 100% COMPLETE (2026-08-07).** First run (`--force` parallel launch, no singleton conflict): 1189/1189 shards, 3,160 rows, but 557/1189 `MORPHO/ETHEREUM` shards hit a legacy-schema gap (fold script expected a `token` column; MORPHO's isolated-market rows never had one, only `instrument_id`/`instrument_key`) — safely skipped as `missing_source`, not guessed. Root-caused + fixed the script to derive the symbol from `instrument_key` when `token` is absent (`market-tick-data-service@f835f605f`), smoke-tested against a real shard before shipping, then relaunched: VM `backfill-defi-legacy-datatype-fold-20260807-195817` processed the full 1189-shard worklist again (idempotent, `skipped_existing: 3160` correctly recognized the first run's output), **`written: 22,968`, `missing_source: 0`, exit_code=0**. Combined total (3,160 + 22,968 accounting, `skipped_existing` included) exactly matches the **26,128**-row legacy `rate_indices` population measured in the fresh census earlier this session — every legacy row is now accounted for, zero residual. | Check `plans/active/issues/defi_legacy_data_type_names_manifest_migration_scope_2026_08_04.md` for the dex_swaps/rate_indices remainder; `/plans/active/issues/defi_manifest_allow_stale_fallback_incomplete_for_long_pause_2026_08_07.md` for the stale-fallback gap; `/plans/archive/issues/manifestwriter_add_concurrent_duplication_race_2026_08_06.md` for the concurrency fix. Re-launch via `MACHINE_TYPE=e2-standard-8 bash scripts/vm/launch-backfill-defi-legacy-datatype-fold-vm.sh --only dex_swaps` (WITHOUT `--allow-stale-fallback`) once the consolidator has caught up. |
| 5   | Retire `dex_pool_fees` (repoint strategy reader)                                                                                                                                                                                             | Sub-agent (background)                                                                                                                   | **DONE.** DIAG confirmed CURVE's `dex_pool_state` already carries real subgraph fees_usd/volume_usd/fee_rate_bps AND the `dex_pool_fees` corpus itself was 0 objects for its entire lifetime — retirement is risk-free. Repointed `canonical_dex_pool_provider.py`, deleted `materialize_dex_pool_fees.py`, 8/8 tests pass. strategy-service commit `f7ca12767a51dc5e7d9327b1d0b875dc5454bb8a`. Found + filed a SEPARATE genuine BALANCER writer-schema gap (`swap_volume`/`swap_fees` vs `tvl_usd`/`volume_usd`, cumulative not daily) as `plans/active/issues/defi_balancer_dex_pool_state_writer_schema_mismatch_2026_08_04.md` — does not block this item. **That gap is now ALSO fixed (2026-08-07, dispatched sub-agent)**: writer emits canonical Messari-shape columns + daily-delta fee/volume instead of silently reading `fee_apy_bps=0` (`market-tick-data-service@2f7d7840` + `@63199601`), verified via a new strategy-service test, PM doc todos 1-4 (DESIGN/IMPL/VERIFY/AUDIT) flipped done (`unified-trading-pm@fe5e7b7d57`). Two follow-ups surfaced and filed as new todos in that issue doc: delete the now-dead `_parse_curve` (latent variant of the same naming-mismatch bug, unreferenced in production) and an `[HUMAN]` call on whether to backfill historical BALANCER days (out of scope for the go-forward fix).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Check `/plans/archive/issues/defi_dex_pool_fees_retirement_recommendation_2026_08_04.md` (all todos closed); `git log strategy-service` for the commit above                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 6   | Root-cause + fix HYPERLIQUID/EXTENDED/LIGHTER/BLAZESTAKE residue                                                                                                                                                                             | Sub-agent (background) + interactive session 2026-08-05/06                                                                               | **All 4 now fully resolved (historical data hygiene complete for all).** HYPERLIQUID: live, actively-read, do-not-touch (no fix needed). EXTENDED/LIGHTER: stale catalogue rows purged (`instruments-service@14746732`). BLAZESTAKE: writer fixed (row 7), AND the full historical corpus (1,406 objects, 1,318 days, 2022-12-14..2026-08-05) relabeled + retired to canonical `SOLBLAZE-SOLANA` (`market-tick-data-service` one-off `relabel_retire_blazestake_venue_2026_08_06.py`, run against prod 2026-08-06). Verified directly against the canonical index: `BLAZESTAKE` captured=0, `SOLBLAZE-SOLANA` captured=1,319 (fully consolidated). The only remaining open item is a genuinely separate operator design decision (which adapter owns `SOLBLAZE-SOLANA` production going forward, i.e. who writes NEW data) -- the data hygiene itself does not depend on that decision and is done.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Check `plans/archive/2026_08/issues/defi_hyperliquid_residual_manifest_rows_2026_08_04.md` (status: open, only for the operator-gated production-ownership question)                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 7   | GMX catalogue rebuild (stop the daily re-seed) + KAMINO_LENDING                                                                                                                                                                              | Interactive session 2026-08-05                                                                                                           | **KAMINO_LENDING now FULLY DONE (both writer + full historical backfill + retirement).** Root-caused a currently-LIVE bug in `risk_params_handler.py`/`lending_indices_handler.py` writing bare `venue=protocol` instead of canonical `KAMINO-SOLANA` -- fixed via a shared `canonical_lending_venue()` helper (`market-tick-data-service@bd153821`), also caught + fixed `solend`/`marginfi` (same bug). Backfilled ALL historical KAMINO_LENDING captured rows to canonical venue -- extended past the original 64-object/4-day scope to the full 565-row/5-day population (the 5th day, 2026-08-05, exists because `bd153821` has not yet reached `main`/the deployed Cloud Run Job image, stuck behind a pre-existing CI capacity backlog on the LDR->main promote -- that day's cron run at 00:45 UTC used pre-fix code; every subsequent day will keep adding rows here until the deploy actually lands). `relabel_kamino_lending_venue_2026_08_05.py --apply` (80 objects/565 rows copy-relabeled to KAMINO-SOLANA) then `retire_kamino_lending_legacy_venue_2026_08_05.py --apply` (the relabel script was additive-only, never retired the old rows despite its docstring claiming otherwise -- fixed + new companion script added) retired all 565 legacy rows. Verified directly against the freshly-written index: **0 remaining captured venue=KAMINO_LENDING rows.** Both scripts + a new `defi-kamino-lending-retire` launcher category shipped (`market-tick-data-service`, `deployment-service@...`). GMX half: not re-checked this session (was already confirmed clean earlier 2026-08-05, see the GMX retraction entry above).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | See `plans/archive/2026_08/issues/defi_hyperliquid_residual_manifest_rows_2026_08_04.md` for the parent BLAZESTAKE/EXTENDED/LIGHTER/HYPERLIQUID investigation this reused the same session's findings from. Re-check the manifest for venue=KAMINO_LENDING captured rows after `bd153821` finally deploys -- if the backlog persists past 2026-08-06, another day's worth of rows will need the same retire script re-run.                                                                                                                                                               |
| 8   | Register `perp_mark_price` + backfill                                                                                                                                                                                                        | Interactive session 2026-08-05                                                                                                           | **DONE.** Registered as canonical data_type + SchemaContract (`unified-api-contracts@75245222`) — pure manifest-visibility fix, zero live-reader risk (confirmed nothing reads this data_type today, unlike `perp_daily_ctx`). Backfill script (`unified-trading-pm/scripts/migration/register_perp_mark_price_manifest_backfill_2026_08_05.py`) found exactly 316 days / 22,690 objects (matches the archived 2026-07-13 migration doc's own "316d" figure exactly) — registered as 316 manifest rows, verified via a fresh per-VM shard read. Two bugs caught+fixed along the way: the first apply attempt silently failed to persist (forgot `MANIFEST_PER_VM_SHARDS=true`, the script's own final log line printed "success" regardless of the underlying write's outcome — re-ran correctly the second time); the original per-file row-count design would have taken 60+ minutes (22,690 sequential downloads) — fixed to use `len(blob_names)` directly, reusing the SAME verified 1-row/file shortcut the perp_daily_ctx precedent established for this identical HL corpus.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Verify: `unified-api-contracts` `DATA_TYPES_BY_ASSET_GROUP["defi"]` has `perp_mark_price`; fresh coverage.json shows 316 manifest rows registered.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 9   | Real fix (not permanent-exception) for the 5 CeFi-venue DeFi-bucket duplicates                                                                                                                                                               | Sub-agent (background)                                                                                                                   | **Label was stale — actually RESOLVED, re-scoped not fixed.** P2(b) investigation concluded (multiple prior sessions) that this is `no-still-authoritative` — a live-reader dependency (CARRY_BASIS_PERP/CARRY_FUNDING_DISPERSION) means deleting would break the live path; disposition flipped from "safe cleanup" to "do not delete." Checkbox closed `[x]`. Nothing is currently dispatched/running against this doc.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Check `defi_cefi_venue_chain_axis_contamination_2026_07_28.md`'s P2(b) item (closed, disposition `no-still-authoritative`)                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

## Already shipped this session (durable, verified, not at risk)

- `perp_daily_ctx` registered + 1,158 manifest rows backfilled — `unified-api-contracts@17b1cf21`,
  `features-service@c678f0fd`, `unified-trading-pm@ccbef0315`.
- Stale `_schema_spec_defi.py` docstring fix — `unified-api-contracts@ab4693de` / `520baddc` (two related commits).
- GMX GCS+manifest purge — 90 objects deleted, 660 manifest rows dropped, cron resumed. Corrected a stale "already
  complete" claim in the archived removal doc.
- GMX 4 residual-row root cause found (stale `catalog.parquet`, daily re-seed) — feeds directly into item 7 above.
- **GMX "3,305 residual manifest rows" finding (2026-08-05, interactive session continuation) — investigated, RETRACTED,
  not real.** Surfaced while ad-hoc-querying the DeFi tick manifest during the gas_fees purge (item 1); the first query
  hit the wrong bucket (`instruments-store-defi-prd-*`, the catalogue bucket, instead of `market-data-tick-defi-prd-*`,
  the tick bucket the purge actually touches), and a second, correct-bucket query still returned inconsistent counts (0,
  then an error, then 0) while item 1's purge attempts were concurrently reading/mutating the same manifest. Once the
  manifest was fully quiescent (all further purge launches stopped, see
  `/plans/active/issues/defi_gas_fees_legacy_purge_manifest_step_blocked_vm_infra_flakiness_2026_08_05.md`), two fresh
  independent checks both returned **0 GMX rows**, and a fresh, independently-triggered honest-coverage rollup
  (`gs://central-element-323112-honest-coverage/2026-08-05/coverage.json`, `generated_at: 2026-08-05T14:42:13Z`,
  `partial: false`, all 5 asset groups measured) confirms `GMX` absent from `by_chain.defi` — matching the 4-row-case
  fix above, no new purge needed. Lesson: a manifest read taken WHILE a concurrent writer/purger is active is not
  trustworthy evidence either way; re-check once quiescent before filing a residual-row finding.
- 4 new issue docs filed: `defi_legacy_data_type_names_manifest_migration_scope_2026_08_04.md`,
  `defi_dex_pool_fees_retirement_recommendation_2026_08_04.md`, `defi_hyperliquid_residual_manifest_rows_2026_08_04.md`,
  `defi_manifest_column_fill_regression_from_gmx_purge_forced_full_merge_2026_08_04.md`.
- **2026-08-07 session**: Balancer `dex_pool_state` writer-schema fix DONE (see row 5 note above). `dex_swaps` fold
  script hardened — InstrumentType casing crash fixed (`market-tick-data-service@513a83dd`) and a real `ManifestWriter`
  concurrent-duplication race found+fixed+stress-tested (`market-tick-data-service@94e625c7`, bundled QG `MAX_DURATION`
  600→800 fix). A `--allow-stale-fallback` launcher flag was added (`deployment-service@cb6397815`), found to be
  data-INCOMPLETE for a long-paused consolidator (caught via a suspicious 260-shard result vs ~27,549 expected), and
  corrected (`deployment-service@3dd83fef8`) — the run made under that flag is explicitly disowned, not a valid
  completion. gas_fees manifest-row purge: the previously-blocking VM-boot `gsutil` hang did NOT recur; found and fixed
  a real OOM (machine-size upsize) but then hit a genuinely NEW, not-yet-root-caused reap-mid-write failure — stopped
  after 3 distinct failure modes per this workspace's own "don't guess a 4th time" precedent. 3 new issue docs filed:
  `manifestwriter_add_concurrent_duplication_race_2026_08_06.md` (library-level `RLock` fix recommended, not
  implemented), `lc_verify_tarball_freshness_auto_mode_silent_dirty_skip_2026_08_06.md` (auto-mode gap hit twice, not
  fixed), `defi_manifest_allow_stale_fallback_incomplete_for_long_pause_2026_08_07.md` (the stale-fallback completeness
  gap). aiohttp dependency-alignment regression that was blocking ALL `unified-trading-pm` pushes confirmed resolved
  fleet-wide (`instruments-service` HEAD carries `d07b24b8`, `trading-agent-service` self-healed by another session).

## Big finding still open — needs operator attention independent of everything above

**`defi_manifest_column_fill_regression_from_gmx_purge_forced_full_merge_2026_08_04.md`** — the GMX purge's forced
full-merge triggered a CRITICAL `MANIFEST_COLUMN_FILL_REGRESSION` on 11 unrelated columns across the whole 42M-row DeFi
manifest (73.92%→71.71%), now live in production, NOT root-caused or remediated. This is unrelated to the
zero-non-canonical dispatch above and should not get lost in the volume of that work.

## Lessons from this checkpoint (Step 6 — carry forward, don't re-learn)

- **A "silenced by a comparison-exception" badge does not mean small scale.** POOL vs pool looked like a cosmetic P3
  until actually queried — it's 1.9M rows, comparable in size to the dex_pools/dex_swaps migration. Always get the real
  row count via a bounded manifest read before triaging severity by UI-badge visibility alone.
- **"Scope only, don't execute" was the wrong default for this dispatch.** The operator's standing principle this
  session: no axis gets a permanently-accepted duplicate; a finding that can be verified-then-executed should be, not
  filed as a recommendation. The exception that still holds: a change touching the live paper-trading determinism
  guarantee (`CanonicalPerpFundingProvider`) needs a stricter, evidence-gated bar (see item 9) — that is not laziness,
  it's a different, harder invariant.
- **Sub-agents that background their own long-running verification and then go idle without a live watchdog will stall
  silently across multiple turns** — the gas_fees sub-agent did this twice before the dispatching session took over
  execution directly. If a sub-agent reports "waiting for my own watchdog" and later shows `no active task` on resume,
  it was never actually running anything — nudge with a concrete, synchronous, narrated-inline instruction, or take over
  directly if it stalls a second time.
- **Composite-venue and non-canonical findings recur across at least 3 structurally-different populations** this session
  found (the already-folded 9-venue no-chain-variation population from 2026-07-24; the new 22-venue
  underscored-multichain population from today; the POOL-casing population) — when auditing "is X folded", verify the
  EXACT venue-name list matches, don't assume one fold covers a superficially-similar-looking one.

## Next steps for whoever resumes this (if the dispatching session's context is lost before reconciliation)

1. Check each row in the in-flight table above via the cited "how to check" method — do NOT assume anything landed.
2. For anything still not done, re-dispatch or execute directly following the same patterns already proven this session
   (five-part-proof + fresh soft-delete check for deletes; the `perp_daily_ctx` registration pattern for new data_types;
   the `fold_legacy_composite_venue_objects_2026_07_31.py` pattern for composite-venue folds).
3. Once every row above is genuinely done, re-pull the live distinct-values panel and confirm zero non-canonical remains
   (or document the specific, evidenced exceptions that genuinely can't reach zero yet, e.g. item 9 if it's still gated
   on the CeFi capture outage resolving).
4. Archive this tracking doc once superseded by a clean final state — it is a dispatch-tracking doc, not a permanent
   record.

## Deferred work after 2026-08-07 (pre-compact checkpoint)

| Item                                                                            | State / why deferred                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Blocked on                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `dex_swaps` migration — **GENUINELY COMPLETE 2026-08-08 ~19:02 UTC**            | VM `backfill-defi-legacy-datatype-fold-20260808-024818` finished clean: `done (apply=True)`, `totals={'written': 1469224, 'skipped_existing': 1974714, 'missing_source': 3344, 'manifest_registered': 1469224, 'shards_ok': 27549}` — full `27,549/27,549` shard worklist, `exit_code=0`, `DEPLOYMENT_COMPLETED`. Confirmed via a direct GCS read of the VM's own `run.log` tee (ADC-based, `gcloud` CLI blocked this session — see the reauth row above), not a self-report trusted blind. VM self-deleted per `VM_SHUTDOWN_ON_COMPLETION=true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Next: launch `dex_pools` fold VM (`--only dex_pools`, singleton lock now free) — **blocked on the `[OPERATOR]` gcloud CLI reauth row above**, cannot launch via any workaround this session.                                                                     |
| `rate_indices` MORPHO/ETHEREUM residual (557 shards)                            | **FIXED + shipped 2026-08-07** (`market-tick-data-service@f835f605f`) — fold script now derives the symbol from `instrument_key` (strips the `VENUE-CHAIN:LENDING_MARKET:` prefix, replaces the remaining `:` with `-`) when the primary `token` column is absent; smoke-tested against a real shard (20/20 instruments written, 0 missing) before shipping. Re-launch VM in flight to pick up the 557 shards.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | —                                                                                                                                                                                                                                                                |
| gas_fees manifest-row purge (12,425 phantom rows)                               | **Not done** — GCS side still 100% complete/verified; old VM-boot `gsutil` hang did not recur, this session's OOM finding is fixed, but a genuinely NEW reap-mid-write failure blocks the manifest step, not yet root-caused.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Nobody — see `/plans/active/issues/defi_gas_fees_legacy_purge_manifest_step_blocked_vm_infra_flakiness_2026_08_05.md`; stopped after 3 distinct failure modes per this workspace's own precedent — needs fresh diagnosis.                                        |
| Balancer `dex_pool_state` writer-schema gap                                     | **DONE 2026-08-07** — writer fix + verification shipped (`market-tick-data-service@2f7d7840`/`@63199601`, `unified-trading-pm@fe5e7b7d57`). Residual follow-ups (dead `_parse_curve`, historical-backfill decision) live in the issue doc's own todos.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —                                                                                                                                                                                                                                                                |
| BLAZESTAKE production ownership (who writes NEW SOLBLAZE-SOLANA data)           | **Operator-owned** — historical data hygiene is fully done; this is a genuine human design decision, not work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Operator decision.                                                                                                                                                                                                                                               |
| GMX manifest-column-fill regression (11 columns, 73.92%→71.71%)                 | **Not done** — CRITICAL big finding, root-caused as benign (candidate b: legitimate dilution from net-new rows) by a fleet sub-agent but not yet formally closed out in its own issue doc.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Nobody — see `/plans/archive/issues/defi_manifest_column_fill_regression_from_gmx_purge_forced_full_merge_2026_08_04.md`.                                                                                                                                        |
| `ManifestWriter.add()` concurrent-duplication race — library fix                | **Not done** — fixed at script level only (dex_swaps fold script); recommended library fix (wire `self._write_lock` as `RLock`) unimplemented, so any OTHER concurrent caller remains exposed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Nobody — see `/plans/archive/issues/manifestwriter_add_concurrent_duplication_race_2026_08_06.md`.                                                                                                                                                               |
| `lc_verify_tarball_freshness` auto-mode silent dirty-skip gap                   | **DONE 2026-08-07** — fixed by another session/AO-dispatched worker (`deployment-service@450b212b`, 16:17 UTC), not by me; live-verified in this session's own next rate_indices launch attempt (correctly required a real republish before proceeding). Removed from open work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | —                                                                                                                                                                                                                                                                |
| `--allow-stale-fallback` completeness-detection gap                             | **Not done** — flag now correctly documented as memory-safe-not-data-complete; no code-level detection/warning exists to stop a future caller repeating this session's mistake.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Nobody — see `/plans/active/issues/defi_manifest_allow_stale_fallback_incomplete_for_long_pause_2026_08_07.md`.                                                                                                                                                  |
| `[OPERATOR]` gcloud CLI auth expired mid-session (2026-08-07 ~22:27 UTC)        | **Blocks VM launches + scheduler control until a human runs `gcloud auth login` interactively** (2FA/security-key reauth). Python/ADC reads unaffected. See Progress Log entry for the near-miss this caused (a wait-loop misread the failure as VM-terminated).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `[OPERATOR]` — needed before `dex_swaps` can be relaunched or the consolidator cron resumed, once the rebuild VM finishes.                                                                                                                                       |
| `[OPERATOR]` Manifest purge: retired legacy_duplicate rows + `AAVEV3` seed rows | **Prepared, NOT agent-executable — correction to this table's earlier framing.** `dex_pools`(454,014)/`dex_pool_fees`(21)/`KAMINO_LENDING`(565)/`BLAZESTAKE`(1,405)/POOL-uppercase(2,328) all still show on the exact API backing the deployment-ui panel because capture_status-flip retirement leaves the row's own venue/data_type column unchanged. `AAVEV3` (46,300 rows) root-caused: all `capture_status=empty_confirmed` (inert bookkeeping, zero real captured data, seeded 2018-2023 under the legacy alias spelling) — low-risk, no live-writer bug. The proven pattern (`market-tick-data-service/scripts/one_offs/purge_gas_fees_legacy_venue_prefixes_2026_08_04.py`, adapted from `purge_gmx_venue_removal_2026_07_25.py`) is EXPLICIT in both scripts' own docstrings: **"HUMAN-EXECUTED ONLY... no agent may invoke `--apply` against this bucket autonomously"** (delete-safety-protocol hard-stop #1) — this is a genuine manifest-ROW-DELETE (index rewrite dropping rows), materially different from the capture_status-flip "retirements" done autonomously elsewhere in this doc (fully reversible, no row removed). The GMX manifest purge in "Already shipped" below (660 rows dropped) used this same human-gated path. An agent may prepare + `--dry-run` characterize the scope; only a human runs `--apply`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `[OPERATOR]` — dry-run characterization safe to prepare now; `--apply` needs a human, after the rebuild-VM-terminal-state gate (index-rewrite race risk) same as `dex_swaps`.                                                                                    |
| `dex_pools` legacy data_type — has NOT been folded (correction, 2026-08-08)     | **Not started.** The currently-running fold VM was launched with `fold_legacy_dex_pools_swaps_rate_indices_2026_08_04.py --only dex_swaps` specifically — `dex_pools` was never included in this run despite the script's name. `rate_indices` finished before this session; `dex_swaps` is ~85%+ through as of 2026-08-08 09:36 UTC. The row 205 purge above only removes ALREADY-retired `dex_pools` rows — it does not migrate the still-primary legacy rows to the canonical `dex_pool_state` data_type. Needs its own VM launch (`--only dex_pools`) once `dex_swaps` finishes (sequenced, not parallel — same script/bucket, avoids the write-race class already hit once this epic per the `ManifestWriter.add()` row above).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `dex_swaps` finishing first (sequenced), then a fresh launch.                                                                                                                                                                                                    |
| Balancer composite-venue naming (`BALANCER-{BASE,ETHEREUM,OPTIMISM,POLYGON}`)   | **CORRECTED 2026-08-08 (sub-agent dispatch) — this is NOT a fresh, MTDS-fold-eligible defect. It is the exact same population already investigated and RESOLVED in `/plans/archive/issues/defi_underscored_multichain_composite_venue_fold_2026_08_04.md` (2026-08-05, status: resolved) — the row above mis-framed it as "NEW" without cross-referencing that doc.** Re-verified live today via a direct manifest census: 941 captured rows (BALANCER-BASE=245/BALANCER-ETHEREUM=227/BALANCER-OPTIMISM=236/BALANCER-POLYGON=233), all `data_type=dex_pool_swaps`, all `instrument_type=pool`, all `chain=None`, all `pipeline_mode=batch_onchain_subgraph` — same writer signature (`service_name=market-data-processing-service`, `source=onchain_subgraph`) as the archived doc's own provenance trace. Row count is lower than that doc's 2026-08-04 honest-coverage-rollup snapshot (2,002/3,004/2,135/2,758=9,899 for these 4 venues) — not re-investigated, secondary to the finding below. **Direct GCS probes run today (read-only) confirm the archived doc's root cause still holds**: 0/4 venues have ANY object under MTDS's canonical `raw_tick_data/.../venue={V}/` prefix (checked at each venue's own sample day+pipeline_mode, empty every time); all 4 venues have real, dated objects under market-data-processing-service's `processed_candles/by_date/day={D}/pipeline_mode=batch_onchain_subgraph/timeframe={TF}/data_type=dex_pool_swaps/instrument_type=POOL/venue={V}/{V}:POOL:{pool_address}.parquet` (7 timeframes each: 15s/1m/5m/15m/1h/4h/1d) — MDPS-owned derived CANDLE data from its already-completed one-time `backfill_defi_dex_pool_swaps_source_correction.py` campaign, not MTDS raw tick data. **Why no MTDS-scoped fold script is possible or correct**: `write_defi_rows()` (the canonical write path) only knows MTDS's `raw_tick_data/` partition convention — there is no raw-tick source object to copy-and-relabel FROM (confirmed empty, not just unfound), and synthesizing a "raw tick" object from MDPS's derived candle bytes would fabricate incorrect data under the wrong data product rather than fix a naming defect. The archived doc's own disposition ("no fold, no purge, no fix needed — the manifest rows are correct") already stands; its one genuinely open question — should `processed_candles`-layer venues be venue/chain-split at MDPS's write time, and/or should the distinct-values panel exempt processed_candles-layer venues from the raw_tick_data canonicalisation rule — is cross-repo (`market-data-processing-service` write path + `deployment-api` panel logic), **outside market-tick-data-service's repo scope** (this dispatch's only editable repos were market-tick-data-service/unified-trading-pm) and was never a same-repo-scriptable fix. No script written, nothing shipped, no VM needed — treat this row as closed-via-cross-reference, not outstanding MTDS work. | `[OPERATOR]`/cross-repo — if the residual panel-scoping or MDPS-writer-registration question is worth fixing, it needs a new issue doc scoped to market-data-processing-service/deployment-api, not an MTDS todo.                                                |
| `BINANCE-FUTURES`/`BITFINEX-FUTURES` under DeFi's Distinct Values panel         | **Likely already resolved (2026-08-08)** — the 2026-08-05 honest-coverage rollup (what the screenshot shows) lists these under DeFi, but a live manifest query today found **zero rows** for either venue in the DeFi bucket. Probably cleaned up by other work since 08-05. Not re-verified against a fresh rollup yet — should self-clear once one runs; flag if it doesn't.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | A fresh rollup regen (already planned as this doc's end state) — re-check then, don't assume.                                                                                                                                                                    |
| `AAVEV3` bare-alias regression (46,300 rows) — **CODE FIX SHIPPED 2026-08-08**  | **Dormant historical batch artifact, NOT a live/growing writer bug — but the underlying code defect is unfixed.** Sub-agent investigation (read-only) confirmed: all 46,300 rows are `capture_status=empty_confirmed`, `chain=ETHEREUM`, dated 2018-01-01→2023-01-26 (the day before AAVE_V3-ETHEREUM's registered 2023-01-27 launch), 25 data_types × 1,852 rows each, single identical `written_at=2026-08-05T04:08:15.957401+00:00` (one bulk batch), `service_name=instruments-service`. 0 GCS objects under any `AAVEV3` path — pure manifest bookkeeping, no backing data. **Mechanism**: `unified-api-contracts/unified_api_contracts/registry/chain_env.py:199` carries `("ETHEREUM", "AAVEV3"): "2023-01-27"` as a literal duplicate dict key beside the canonical `("ETHEREUM", "AAVE_V3")` entry (line 198, "alias: no-underscore form used by some callers"); `instruments-service/scripts/enumerate_expected_universe.py::_yield_v2_defi_pre_launch_rows` (~line 1476) iterates `PROTOCOL_LAUNCH_DATES.items()` directly with `venue_label = protocol.upper()` and zero alias canonicalization, so the alias key seeds its own full pre-launch placeholder sweep. Rules out both original hypotheses (live writer bug; rebuild VM surfacing old GCS objects — chronologically impossible, rebuild started 2026-08-06, a day after these rows' `written_at`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Fixed `instruments-service@2b2e9f124` (canonicalises + dedups, regression test added) — see `/plans/active/issues/defi_aavev3_bare_alias_enumerator_bug_2026_08_08.md`; `[OPERATOR]` historical-row purge + `[DESIGN]` alias-dict-key question still open there. |
| `[OPERATOR]` gcloud CLI auth expired AGAIN (2026-08-08 ~17:06 UTC, recurrence)  | Same class as the 2026-08-07 ~22:27 UTC expiry (resolved on-demand at 03:20 UTC that session) — recurred ~14h later. `gcloud compute instances describe`/`ssh` both fail "Reauthentication failed. cannot prompt during non-interactive execution." — needs a human `gcloud auth login` (2FA/security-key), cannot complete non-interactively. **Not blocking monitoring** — both VMs confirmed genuinely `RUNNING` via ADC-based `google.cloud.compute_v1.InstancesClient` + a direct GCS read of each VM's `run.log` tee (`gs://deployment-scripts-central-element-323112/vm-logs/<vm-name>/run.log`), both unaffected by the CLI reauth gap, both showing fresh log activity. **Blocks the actual `dex_pools` VM launch + consolidator-cron resume** once both VMs finish. Self-service attempted, not completed: the previously-identified durable fix (activate the `cloudstorage@central-element-323112.iam.gserviceaccount.com` service-account key) failed — `gcloud auth list` shows the account name but no valid cached credential (`invalid_grant: account not found`), and finding its actual key file needs the `secretmanager` package (not installed here) plus digging into a shared host's credential store — an already-documented, operator-acknowledged tradeoff (they chose on-demand reauth over the durable SA-key setup once before), not a gap worth forcing a workaround for.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `[OPERATOR]` — a human `gcloud auth login` (or completing SA-key activation with the real key file) unblocks the `dex_pools` launch + consolidator-cron resume; until then this session can monitor both VMs to completion but not act on it.                    |

**Recommended next item (2026-08-08 ~19:02 UTC update)**: `rate_indices` and `dex_swaps` are both now genuinely
complete. The next real step is launching the `dex_pools` fold VM (`--only dex_pools`, singleton lock now free) —
**blocked on the `[OPERATOR]` gcloud CLI reauth row above**, not on anything else. Once a human re-authenticates: launch
`dex_pools`, wait for it + the still-running `canonical-migration-defi-rebuild` VM to reach terminal state, then resume
the manifest consolidator cron and trigger a fresh honest-coverage rollup (this doc's stated end state).

## Todos

_(formalized 2026-08-10 from prose in the "REVISED completion sequencing" and Progress Log below — see that section for
full evidence/citations. Everything else in the Deferred-work table and Progress Log below already has its own
delegated tracking doc with real checkboxes — not duplicated here.)_

- [ ] [DATA] P2. **Retire POOL (uppercase `instrument_type`) / `rate_indices` / `dex_pool_fees` legacy manifest rows.**
      Per the 2026-08-10 "REVISED completion sequencing" steps 3-5 (Progress Log below): once
      `defi_track01_per_instrument_and_canon_id_2026_07_24.md`'s R3 rebuild VM reaches a genuinely stable terminal
      state (not another resource-exhaustion kill — see that doc's own R3 item for the current relaunch decision, gate
      on it rather than duplicating its tracking), re-measure all three counts live (must be STABLE across two queries
      a few minutes apart before trusting them), pause the manifest consolidator cron
      (`uts-prod-manifest-consolidator-market-data-defi-cron`, `asia-northeast1`), retire each via the proven
      reversible `capture_status: captured→attempted_failed` pattern (mirror
      `retire_dex_pools_legacy_captured_rows_2026_08_05.py` / `retire_dex_swaps_legacy_captured_rows_2026_08_09.py`),
      resume the consolidator, then trigger a fresh `measure_honest_coverage.py` rollup and re-check the Distinct
      Values panel. Repo: market-tick-data-service.
- [ ] [DATA] P3. **Cross-check `instrument_type=spot_pair` (9,802 rows) against `defi-canonical-naming-ssot.md`'s
      locked instrument_type list** — flagged 2026-08-07 ("not yet cross-checked... needs a quick check before
      assuming drift vs. legitimate") but never followed up.
- [ ] [BACKEND] P3. **Fix the Distinct Values panel's `<blank>` `instrument_type` badge to exclude `empty_confirmed`
      rows.** Currently over-reports ~5.35M blank rows when only ~58 `captured` rows are a genuine gap (the rest are
      legitimate honest-absence markers, ~212,800-215,600 per data_type, consistent with one empty-marker per missing
      cell). Needs a `_distinct_values.py` / `measure_honest_coverage.py` change to exclude `empty_confirmed` from the
      blank-key check specifically (mirrors the `capture_status != "attempted_failed"` filter already shipped
      2026-08-10 for the enumeration-key bug, `instruments-service@8b59e8ba2`). Repo: instruments-service or
      deployment-api.

## Progress Log

- **2026-08-08 (sub-agent dispatch, Balancer composite-venue scope check)**: dispatched to build/dry-run/ship a fold
  script for the "Balancer composite-venue naming" Deferred-work row below (`BALANCER-{BASE,ETHEREUM,OPTIMISM,POLYGON}`,
  941 rows). Per the dispatch's own instructions, confirmed full scope FIRST (live manifest census across all 4 venues
  - all data_types, then direct GCS prefix probes) before writing any code — this caught a real problem: **the 941-row
    population is the SAME population already investigated and RESOLVED in
    `/plans/archive/issues/defi_underscored_multichain_composite_venue_fold_2026_08_04.md`** (2026-08-05,
    `trace_composite_venue_provenance_2026_08_05.py`'s own VM-run provenance trace already identified the writer as
    `market-data-processing-service`, real backing data confirmed under MDPS's `processed_candles/` derived-candle
    prefix, disposition "no fold, no purge, no fix needed"). The 2026-08-08 entry below (re-flagging this as "NEW")
    never cross-referenced that archived doc. Re-verified live today: 0/4 venues have any object under MTDS's
    `raw_tick_data/.../venue={V}/` prefix; all 4 have real, dated candle objects (7 timeframes each) under MDPS's
    `processed_candles/` prefix in the SAME bucket. Conclusion: no MTDS-scoped fold script is possible or correct —
    `write_defi_rows()` only knows the `raw_tick_data/` convention, there is no raw-tick source object to fold from, and
    synthesizing one from MDPS's derived-candle bytes would fabricate incorrect data rather than fix a naming defect.
    Did NOT write or ship a script (would have been code against a false premise, the same trap the original
    investigation's first pass fell into before its own VM trace corrected it). Corrected the Deferred-work row below in
    place rather than leaving the stale "NEW finding" framing to mislead a future dispatch into repeating this. No
    commit needed in `market-tick-data-service` (nothing built there this pass); this doc edit is the full deliverable.

- **2026-08-08 (interactive session)**: operator asked directly whether the deployment-ui Distinct Values panel would
  show 0 non-canonical after `dex_swaps` + a fresh rollup — investigated rather than assuming yes. Confirmed live
  (direct manifest query, not the stale 2026-08-05 rollup): Balancer composite-venue naming is a NEW, real, currently-
  untouched gap (941 rows); `dex_pools` legacy fold has never actually started (only `dex_swaps` is running, contrary to
  the fold script's name suggesting both); `BINANCE-FUTURES`/`BITFINEX-FUTURES` under DeFi appear already resolved (0
  live rows) and should self-clear on the next rollup. Answer given: NOT 0 immediately — added as 3 new rows to the
  Deferred work table above. Today's SUSHISWAP/factory-address/HL-ASTER rulings (see other docs) are also decided but
  not yet implemented, so those axes won't clear either until AO actually executes them.
- **na-eligibility-audit 2026-08-04**: KEEP-NA, valid — live operator-session dispatch-tracking checkpoint for an
  in-flight 9-way parallel dispatch, written today under context pressure; 0 tracked `- [ ]` todos by design (its "work
  items" are a status table, not a checklist). Explicitly not archival-eligible yet (own § 4: "Archive this tracking doc
  once superseded by a clean final state") and not AO-dispatchable (every row needs a live status check a
  human/dispatching session must reconcile, not a worker-determinable outcome).
- **context-scout 2026-08-05**: refreshed context_scope (5 entries) -- swapped in the still-open big-finding regression
  issue doc + the blocked gas_fees manifest-purge issue doc, the two live operational threads this checkpoint doc most
  needs a resumer to read first.
- **context-scout 2026-08-07**: refreshed context_scope (6 entries) -- added
  `defi_legacy_data_type_names_manifest_migration_scope_2026_08_04.md`, the tracker for `dex_swaps`/`rate_indices`, the
  doc's own "Recommended next item" and the largest remaining real-data gap in the Deferred-work table.
- **interactive session 2026-08-05/06**: rows 2 (POOL casing), 6 (BLAZESTAKE), 7 (KAMINO_LENDING) driven to genuine
  completion, each verified directly against the canonical index rather than trusted from a script's own success log.
  Along the way, found and fixed a real cross-cutting infra bug unrelated to any single row: the DeFi bucket's manifest
  consolidator was firing a false "SILENT STALL" CRITICAL alert on every single merge cycle (its alert threshold was
  calibrated for a 300s lock TTL, but this bucket deliberately overrides it to 4200s because real merges take 18-30 min)
  -- root-caused, fixed (`unified-trading-library@899976c6` + `deployment-service@173afd6e`), and confirmed the
  consolidator itself had been working correctly the whole time; see
  `plans/active/issues/defi_manifest_consolidator_stale_lock_silent_stall_2026_08_05.md` (status: resolved). This
  consolidator fix is what let the POOL/KAMINO/BLAZESTAKE per-VM shards actually reach the canonical index instead of
  sitting stuck.
- **pre-compact checkpoint 2026-08-06**: shipped the 4 outstanding migration scripts (gas_fees delete,
  dex_pools/dex_swaps/rate_indices fold, POOL casing, BLAZESTAKE) + their 2 tests as `market-tick-data-service@1970ef45`
  (`live-defi-rollout`, ahead=0/behind=0 verified). Along the way found + fixed a real, repo-wide-blocking QG issue: the
  STEP 5.101 empty-string-fallback ratchet was failing at HEAD (85 sites vs baseline 79) in 4 files this session never
  touched (`seed_mock_data.py`, 2 sports league_id relocation scripts, `stamp_schema_version_v9_mtds_2026_06_29.py`) —
  confirmed via `git log <baseline-commit>..HEAD -- <files>` that none of those files changed since the baseline was
  pinned, so this was pre-existing drift blocking every quickmerge to this repo, not something introduced by this
  session. All 6 flagged sites were genuinely deliberate falsy-safe patterns (presence/blankness checks, diagnostic
  display sentinels) matching an already-established sibling convention in the same function
  (`stamp_schema_version_v9_mtds_2026_06_29.py:65` already had the marker one line below the unmarked one) — added
  `# noqa: qg-empty-fallback` with a one-line reason to each, confirmed the checker returns to exactly baseline (79),
  same commit. Added the Deferred-work table above; `dex_swaps`/`rate_indices` genuinely not started, and a
  sub-agent-built dex_pools/dex_swaps/rate_indices fold script shipped as code-only, NOT YET run against prod — do not
  run it against dex_swaps without the same per-venue divergence handling used for POOL/dex_pools.
- **na-eligibility-audit 2026-08-07** (tranche=defi): KEEP-NA valid — 0 checkboxes by design (status table); real
  remaining work is prose-only (gas_fees purge blocked on VM infra, dex_swaps/rate_indices migration pending); archival
  trigger (§4) unmet.
- **pre-compact checkpoint 2026-08-07 (continuation session)**: Balancer writer-schema fix driven to genuine completion
  via a dispatched background sub-agent — verified directly (`market-tick-data-service@2f7d7840`/`@63199601`,
  `unified-trading-pm@fe5e7b7d57`), not trusted from the agent's own report. `dex_swaps` fold script hardened with 2
  real fixes (InstrumentType casing crash, `ManifestWriter` concurrent-duplication race) but migration itself still NOT
  complete — a `--allow-stale-fallback` launcher flag I added and used turned out to be data-incomplete for a
  long-paused consolidator; caught this myself via a suspiciously small result (260 shards vs ~27,549 expected),
  root-caused it, corrected the launcher comment, and explicitly disowned that run rather than letting it stand as a
  false completion. gas_fees: old VM-boot hang did not recur, a real OOM was found+fixed, but a new not-yet-root-caused
  reap-mid-write failure appeared — stopped after 3 distinct failure modes rather than guessing a 4th time. 3 new issue
  docs filed (ManifestWriter race, tarball-freshness auto-mode gap, stale-fallback completeness gap) — all 3 recommend
  library-level fixes that remain unimplemented, tracked in the Deferred-work table above. Confirmed the aiohttp
  dependency-alignment regression (blocking all PM pushes) is resolved fleet-wide. Full `/pre-compact` audit run across
  all 6 touched repos: all `ahead=0/behind=0`, no secrets found, no dangling scratchpad references in any committed doc,
  scratchpad contents (diagnostic scripts + QG logs) confirmed disposable/regenerable, nothing promoted.
- **`/autonomous` scope expansion, 2026-08-07 (operator directive, mid-session)**: operator corrected the framing — the
  goal is NOT "make POOL and pool both canonical" or otherwise force fake merges; it's zero non-canonical values via
  genuine per-case disposition (canonicalize-in-place if no true twin exists / fold into an existing twin / purge if
  dead), matching the rigor CEFI/TRADFI already established, "no backfills" (existing-data migration/purge only), and
  driven off the SAME `axis-value-census` API backing the deployment-ui Distinct Values panel the operator
  screenshotted. Directed to run `rate_indices` + other orthogonal work in parallel while `dex_swaps` waits on the
  rebuild VM, and to resume the manifest consolidator + trigger a fresh honest-coverage rollup once done so the UI
  reflects the clean end-state. Work this tick:
  - **`rate_indices` launched** (`--force`, no conflict since nothing else was running) — see row 4 update; DONE modulo
    the 557-shard MORPHO/ETHEREUM schema-gap residual (new deferred item above).
  - **Caught + fixed a live infra bug attempting the launch**: `lc_verify_tarball_freshness`'s (already-fixed-by-
    someone-else, see above) auto-mode correctly refused to launch onto a stale `market-tick-data-service` tarball
    (blocked by the 3 known foreign-WIP dirty files) — stashed the foreign files by name, force-republished the tarball
    (`git_status_clean: true`, sha matches HEAD `2f7d7840`), restored the stash, relaunched clean. Confirms the fix
    shipped by the other session actually works end-to-end, not just in its own unit test.
  - **Framework discovery — should have been read at task start (pre-task plan/issue conflict check gap, self-caught
    mid-session, not by the operator):** this exact class of work already has a governing framework this tracker hadn't
    cross-referenced: `/codex/02-data/reconciliation-finding-taxonomy.md` (21-type finding vocabulary — only
    `legacy_duplicate` and `junk` are ever delete-eligible; a non-canonical value with no twin is MIGRATED/re-keyed,
    never deleted), `/codex/02-data/canonical-cutover-register.md` (per-axis, per-AG `effective_from` dates — DeFi has
    an explicit 2026-07-24 operator carve-out for `instrument_type` casing: **per-value, least-migration-cost**, not the
    cross-AG blanket-UPPERCASE ruling — confirms the earlier POOL→pool fold this session's predecessor did was
    correctly-scoped, not a violation), and `/codex/02-data/non-canonical-path-inventory.md` (the living register —
    update it, don't re-derive from scratch). Added all three + the directly-relevant live plan
    `defi_track01_per_instrument_and_canon_id_2026_07_24.md` to this doc's `context_scope`/`related`. That plan's own
    "Manifest instrument_type case + venue-spelling unify" todo (resolved 2026-07-24) already found, via a fresh live
    census at the time: **every DeFi `instrument_type` value was already 100% one casing (unanimous lowercase, 0
    exceptions)**, and that `AAVEV3`/`COMPOUND`(bare)/`YEARNV3`/`KAMINO_LENDING` had **0 rows** (already-migrated or
    never-present), while `AAVE` (oracle_prices venue) and `MORPHOVAULTS` (vault_share_price venue) are **genuinely
    distinct, deliberately-registered venues**, not spelling drift of `AAVE_V3`/`MORPHO` — collapsing them would be a
    correctness regression, not a fix. The operator's screenshot (a stale 2026-08-05 honest-coverage-rollup) still
    showed `AAVEV3` non-canonical-badged with a nonzero implied count — this is either a genuine regression since
    2026-07-24 or a rollup-snapshot artifact; needs empirical re-check against a FRESH read (in progress, see below)
    before concluding either way — not assumed.
  - **CEFI/TRADFI playbook researched** (background sub-agent, read-only): shared framework is the taxonomy+register
    above (not a cefi/tradfi-specific invention); disposition criteria are content-verified twin overlap (not
    existence-only) for folds, and a stated cost/evidence argument for canonicalize-in-place; reusable tooling —
    `migration_orphan_sweep.py`, `audit_legacy_gcs_dup_delete_list.py` + `migrate_legacy_twins_from_audit.py`, the
    copy-and-verify-then-purge VM pair pattern; verification bar is "retire ONLY when disposition is EXECUTED and
    evidenced," re-confirmed across several days; root-cause (writer) fixes are MANDATORY before/with historical
    migration (violating this once — the LENDING A_TOKEN/DEBT_TOKEN retire — broke 5+ writers and had to be reversed);
    CEFI/TRADFI are themselves NOT fully done yet (`instrument_type` casing still `migration_pending` for both) — DeFi
    reaching the SAME maturity bar is the target, not a stricter one.
  - **Live axis-value-census read in flight** (calling
    `deployment_api.routes.data_status._axis_census .get_axis_value_census(service="market-tick-data-service", asset_group="defi")`
    directly — the exact function backing the operator's screenshot — bypassing the stale 2026-08-05 rollup cache) to
    get the CURRENT real non-canonical inventory before making any per-value disposition call. Running unusually slowly
    (6+ min for a normally-bounded column-pruned read), plausibly contending with the still-running rebuild VM's own
    heavy GCS traffic on the same bucket — not yet concluded as of this log entry.
  - **Census returned** (76,701,208 total MTDS-bucket rows, ALL capture_status — see finding below on why that matters).
    Cross-checked every flagged value against `defi_track01_per_instrument_and_canon_id_2026_07_24.md`'s prior
    findings + the taxonomy/register framework before touching anything:
    - **Already correctly resolved, no action** — the ~22 small (32-257 row) `PROTOCOL-CHAIN` composite venues
      (`BALANCER-ARBITRUM`, `UNISWAP_V3-BASE`, etc.): this tracker's own row 3 already investigated + closed these as
      real MDPS-owned data, not drift. Bare `SUSHISWAP` (506,137) / `UNISWAP` (7,625): track01's own P2 DECISION
      (2026-07-21) ruled per-pool factory-address resolution required, currently 0/206,107 resolvable (**a genuine
      "don't guess" residual with its own issue doc + follow-up capture-work todo**, not silently dropped — do NOT force
      a merge here). `AAVE` (102,933) / `MORPHOVAULTS` (56,560) / bare `COMPOUND` (15,800, UAC-registered
      `COMPOUND-ETHEREUM`): confirmed genuinely-distinct, deliberately-registered venues (oracle/vault-product/V2
      respectively), not spelling drift of `AAVE_V3`/`MORPHO`/`COMPOUND_V3` — collapsing any of them would be a
      correctness regression per track01's own 2026-07-24 finding.
    - **🔴 Major systemic finding, not yet fixed — "retiring" a row (capture_status flip) does NOT clear it from this
      exact API/panel.** `get_axis_value_census` reads via `read_availability_index(bucket, columns=[...])` with **no
      `capture_status` filter at all** — it counts EVERY row regardless of status. `dex_pools` still shows **454,014**
      (≈ the 453,985 this tracker "retired" captured→attempted_failed, GCS untouched); `KAMINO_LENDING` still shows
      **565** (≈ the 565 retired); `dex_pool_fees` shows 21; `BLAZESTAKE` still shows 1,405 despite "0 remaining
      captured" being independently true. This is exactly the operator's stated complaint — a capture_status flip is
      real, safe, reversible progress on the DATA correctness question, but does NOT achieve a clean panel, because the
      manifest ROW (carrying the old value in its `venue`/`data_type` column) still physically exists. **Fix, not yet
      executed**: for every already-verified `legacy_duplicate` disposition this tracker previously "retired," either
      (a) truly DELETE the phantom manifest row (not the GCS object — the taxonomy's own `legacy_duplicate`/`junk` are
      the two delete-eligible types, and these already cleared that bar when they were first retired) or (b) re-key the
      row's `venue`/`data_type` column to the canonical value instead of status-flipping. (a) is simpler and matches
      what "retire" already half-did. New follow-up todo below.
    - **🔴 Real, unexplained regression, under investigation — `AAVEV3` (bare, no chain) = 46,300 rows**, vs. **0**
      measured by track01's own fresh live census on 2026-07-24. `AAVEV3→AAVE_V3` IS a known, registered alias
      (`unified-api-contracts/unified_api_contracts/registry/defi_venues.py:323`, `venue_mapping.py:596-617`,
      `chain_env.py:199`) with dedicated normalization logic — including inside the currently-running rebuild script
      itself (`rebuild_defi_manifest.py::_canonical_defi_venue`, confirmed correctly maps `AAVEV3→AAVE_V3` by reading
      the code). So this isn't an obvious "nobody canonicalizes this" gap — something is either (a) a live writer
      bypassing the canonicalization path entirely, or (b) the rebuild VM itself surfacing OLD raw-GCS-object content
      that was always there but invisible while the consolidator was paused (the rebuild re-derives the manifest from
      actual GCS object content, not from the old index). **Not yet root-caused** — needs: which pipeline_mode/date
      range these 46,300 rows span (correlate against the rebuild's `last_completed_date` checkpoint to test hypothesis
      (b)), and a live-writer grep for any DeFi AAVE_V3 capture path that could emit the bare alias post-2026-07-24.
      Top-priority next investigation.
    - **`instrument_type=POOL` (uppercase) still shows 2,328 rows** despite this tracker's row 2 claiming "0 remaining
      captured rows with instrument_type=POOL" — consistent with the SAME retired-rows-still-count mechanism above (that
      fold's own verification query filtered `capture_status=='captured'`, which the raw census does not) rather than a
      new problem; will resolve automatically once the systemic fix above lands.
    - **`instrument_type=spot_pair` (9,802) not yet cross-checked** against `defi-canonical-naming-ssot.md`'s locked
      instrument_type list (which doesn't mention it) — needs a quick check before assuming drift vs. legitimate.
  - **New follow-up todos** (added to Deferred-work table below): (1) purge/re-key the already-"retired"
    legacy_duplicate manifest rows so they stop counting on the axis census (dex_pools, dex_pool_fees, KAMINO_LENDING,
    BLAZESTAKE, and dex_swaps once its own migration completes); (2) root-cause the AAVEV3 regression; (3) MORPHO
    rate_indices schema gap (from row 4's update above).
  - **Next**: root-cause AAVEV3, design+run the retired-row purge, keep dex_swaps/rate_indices-MORPHO moving, then
    resume the consolidator cron + trigger a fresh honest-coverage rollup once all land so the deployment-ui panel
    reflects the genuinely clean state — a rollup regeneration is meaningless before the purge lands, since the panel
    would still show the retired-but-uncleared values.
- **`[OPERATOR]` gcloud CLI auth expired mid-session, 2026-08-07 ~22:27 UTC — blocks VM launches + scheduler control
  until a human re-authenticates.** `rate_indices`/MORPHO work above all landed BEFORE this; discovered while verifying
  the rebuild VM's terminal state for the `dex_swaps` relaunch. **Near-miss self-caught, not by the operator**: the
  wait-loop's `gcloud compute instances describe ... 2>/dev/null || echo 'GONE'` pattern silently treats ANY gcloud
  command failure (including this exact auth expiry) as "VM terminated" — it reported `GONE` while the VM's own
  `run.log` showed continuous fresh activity seconds earlier. Cross-checked via ADC-authenticated Python
  (`google.cloud.compute_v1`, ADC uses the SAME account's OAuth refresh token but a different reauth/RAPT path than the
  `gcloud` CLI's own session — still valid) and confirmed the VM is genuinely `RUNNING`; re-armed the wait-loop using
  that same ADC path (a query FAILURE is no longer treated as a terminal-state signal — only a genuine 404 is). **Lesson
  for any future wait-loop in this session or workspace**: `cmd 2>/dev/null || echo <terminal-state>` is unsafe whenever
  `cmd` itself can transiently fail for a reason unrelated to the thing being checked (auth, network, rate-limit) —
  always distinguish "the check failed" from "the check succeeded and returned a terminal answer." **Remaining impact**:
  `gcloud compute instances create` (VM launches, incl. the queued `dex_swaps` relaunch) and
  `gcloud scheduler jobs resume`/`pause` (consolidator cron control) are BLOCKED until a human runs `gcloud auth login`
  interactively (2FA/security-key reauth, cannot be completed non-interactively) — confirmed via
  `gcloud projects describe` failing identically. Python/ADC-based GCS reads + `google.cloud.compute_v1` status checks
  are UNAFFECTED and continuing normally. `dex_swaps` itself remains additionally gated on the rebuild VM's genuine
  completion (unaffected by this, separately tracked) — so this auth gap is not yet the active blocker, but WILL become
  one the moment the rebuild VM finishes, unless resolved first.
- **`dex_swaps` relaunched 2026-08-08 ~03:20 UTC — reconsidered the wait-for-rebuild-VM-termination gate as overly
  conservative.** The rebuild VM (`canonical-migration-defi-rebuild-20260806-223130`) is still genuinely RUNNING (~54%
  through its 2020-2026 date range at last check, `date=2023-10-08`), not finished — but `rate_indices` already PROVED
  this exact operation class (a `ManifestWriter(..., per_vm_shards=True)` fold script, writing to its own namespaced
  per-VM shard, never touching the consolidated index directly) is safe to run in parallel with the rebuild VM: it
  launched and completed twice this session while the rebuild was actively writing, with zero issues. The original "wait
  for full termination" caution was calibrated for the `--allow-stale-fallback` disaster earlier in this saga (a
  genuinely different risk: a full manifest-index-REWRITE racing the rebuild), not for a normal per-VM-shard fold read —
  that distinction got blurred when the caution was first written and never revisited once `rate_indices` proved the
  pattern safe. Operator concurred (explicit ask to unblock now, gcloud CLI's own auth fixed live for exactly this
  purpose). **Operator gcloud CLI auth**: diagnosed as an org-level (`odum-research.com`) reauth policy specifically
  gating Compute/Scheduler-scope commands (a plain `gcloud projects describe` kept working throughout;
  `gcloud compute`/`gcloud scheduler` needed fresh interactive MFA) — NOT a broken credential. Python/ADC-based work was
  unaffected the whole time (confirms the policy is CLI-session-scoped, not account-wide). Durable fix (a
  service-account key for `cloudstorage@central-element-323112.iam.gserviceaccount.com`, already registered but not
  locally activated) was identified but deferred — operator chose to re-auth on-demand instead. Also hit two genuine,
  unrelated local network connectivity blips mid-session (`ConnectTimeoutError` to `storage.googleapis.com` and
  separately `compute.googleapis.com`, both resolved on retry within minutes) — a first launch attempt's log came back
  empty/PID-not-found, most likely killed as a side effect of its parent command being moved to the harness's own
  background-timeout handling rather than a real script failure; relaunched cleanly in an isolated `nohup ... & disown`
  and confirmed genuinely alive via `ps` — but that attempt hit the standard stale-tarball block (the same 3 foreign
  files as always), correctly aborted with no VM created, and was resolved via the proven stash-by-name +
  `create-code-tarballs.sh --force` + stash-pop cycle. **`dex_swaps` VM LAUNCHED successfully**:
  `backfill-defi-legacy-datatype-fold-20260808-024818` (e2-standard-8, SPOT, `asia-northeast1-c`), confirmed `RUNNING`,
  all 3 tarballs fresh at launch (`mtds@f835f605`). Watchdog armed with corrected error-handling (a `gcloud` check
  failure is explicitly NOT treated as terminal, per the earlier near-miss lesson). **Not yet confirmed complete as of
  this checkpoint** — resume by checking
  `gcloud compute instances describe backfill-defi-legacy-datatype-fold-20260808-024818 --zone asia-northeast1-c` or the
  run.log at
  `gs://deployment-scripts-central-element-323112/vm-logs/backfill-defi-legacy-datatype-fold-20260808-024818/run.log`
  directly (never trust a bare PID across a session restart).
- **na-eligibility-audit 2026-08-08 (round7 RECLASSIFY sweep)**: KEEP-NA, valid — 0 tracked `- [ ]` todos by design
  (live operator-session dispatch-tracking checkpoint; its "work items" are an in-flight status table, not a checklist),
  consistent with every prior round (2026-08-04/07). Not archival-eligible yet per its own §4 criterion (multiple rows
  still genuinely in-flight: `dex_swaps` VM running, gas_fees manifest purge blocked, the `AAVEV3`/Balancer/`dex_pools`
  residuals not yet closed). Nothing to reclassify.
- **2026-08-09 — panel-clearing mechanics root-caused: fold ≠ retirement.** The fold scripts (`fold_legacy_*`) are
  COPY-not-move — they write NEW canonical rows alongside the OLD legacy-labeled rows, so completing a fold alone never
  clears the legacy spelling from the Distinct Values panel. A separate RETIREMENT step
  (`capture_status: captured → attempted_failed`, fully reversible, never touches GCS objects or deletes manifest rows —
  same pattern already proven on POOL/KAMINO_LENDING/BLAZESTAKE) is required per legacy label. Operator authorized this
  retirement + a companion axis-census fix ("ok wrt below i give you permision to run the safe dleetd"), scoped narrowly
  to the reversible flip only — a future manifest-row PURGE remains separate, later, operator-only.
  - **dex_pools retirement**: `retire_dex_pools_legacy_captured_rows_2026_08_05.py --apply` — retired 212,767, excluded
    29, round-trip verify passed. **Live-verified**: census `dex_pools: attempted_failed=453,985, captured=29` — the 29
    remaining exactly match the excluded/unsafe count.
  - **dex_swaps retirement**: new script `retire_dex_swaps_legacy_captured_rows_2026_08_09.py` (4-tuple keys, no
    venue/chain narrowing). **Hit + fixed a genuine "wrong vocabulary probed" false-negative**: first dry-run showed
    only 13.9% safe because legacy `dex_swaps` carries a bare hex `instrument_id` while canonical `dex_pool_swaps`
    carries the wrapped `VENUE-CHAIN:TYPE:0xADDRESS` form — literal string matching false-negatived near everything.
    Fixed via an `unwrap_canonical_id` param stripping to the segment after the last `:`; corrected dry-run showed 94.5%
    safe. `--apply`: retired 3,261,073, excluded 198,815, round-trip verify passed, `kept=81,087,880` matched total rows
    exactly. **Live-verified**: census `dex_swaps: attempted_failed=3,261,899, captured=198,815` — `captured` exactly
    matches excluded; the small `attempted_failed` delta (826) is pre-existing genuine capture failures, unrelated.
  - **Consolidator-pause near-miss (self-caught)**: mid-operation discovered the cron was actually ENABLED (wrongly
    assumed already-paused); paused before the dex_pools apply's real write cycle. No actual race occurred. Cron resumed
    post both retirements.
  - **Axis-census filter bug fixed**: `deployment-api/deployment_api/routes/data_status/_axis_census.py` read the
    availability index with NO `capture_status` filter at all, so every retired-and-`attempted_failed` row (this epic's
    whole retirement history: POOL/KAMINO_LENDING/BLAZESTAKE/dex_pools/dex_swaps) kept counting toward the panel's
    non-canonical badges. Fixed with `filters=[("capture_status", "!=", "attempted_failed")]`. **This is the actual
    answer to the operator's original question** — the panel was never going to reflect retirements correctly regardless
    of rollup reruns until this filter landed. Shipped `deployment-api@3d7c7759c`.
  - **Second, unrelated infra bug found + fixed while QG-verifying the above**: `quality-gates.sh` was silently exiting
    2 right after the dep-content gate, no error output — `base-service.sh`'s `grep -oP` UV_VERSION extraction (added
    earlier today by a different slot, `unified-trading-pm@e5697ac5c1`) isn't supported by macOS BSD grep, and ran
    unguarded under `set -e`. Same class as an already-documented `grep -vP` fix 40 lines below it in the same file.
    Fixed via `rg --pcre2` + `|| :`. Blocked **every** local QG run on this host. Shipped
    `unified-trading-pm@3f76c0f57f` (direct push — sanctioned `scripts/**` carve-out, D16).
  - **Two near-misses this session: uncommitted edits silently reverted to HEAD on this shared checkout, twice** (this
    base-service.sh fix, and — mid-write — this very tracker-doc paragraph itself: a first ~65-line addition vanished
    with `git diff` showing byte-identical-to-HEAD and no trace in any of 49 `git stash` entries). Both times the file
    that vanished was NOT touched by whatever incoming commits landed around the same time, so git's own "local changes
    would be overwritten by merge" abort — the safety net `slot-cron-ff-pull.sh`'s first-dirty- tick-proceeds design
    explicitly relies on — would never have triggered either way; that FF-pull cron is therefore a plausible but NOT a
    proven culprit. Root cause unconfirmed. **Mitigation applied both times**: redid the edit and committed immediately
    (seconds, not minutes) rather than leaving it uncommitted. **Recommend a follow-up investigation** (not done here —
    out of scope for finishing this epic's actual data work) into what on this slot-3 checkout is reverting uncommitted
    tracked-file changes; candidates not yet ruled out: `slot-git-status-report.sh` (the sibling 5-min cron) or another
    host-wide hygiene/cleanup automation running unattended on this clone.
  - **Rebuild VM** (`canonical-migration-defi-rebuild-20260806-223130`, zone `asia-northeast1-c`) confirmed healthy and
    progressing at 2026-08-09T11:34 UTC — `date=2024-08-09` in its 2020-2026 walk, 8.18M manifest entries written,
    heartbeat 25s old. Still running, not yet terminal.
  - **Remaining work** (tracked as todos in the Deferred-work table below): commit + ship the new dex_swaps retirement
    script (currently uncommitted in market-tick-data-service); continue monitoring the rebuild VM to terminal state;
    trigger a fresh honest-coverage rollup once the VM is terminal (consolidator already resumed); re-confirm live
    whether HYPERLIQUID (non-canonical chain) and the 45 non-canonical venues still need separate work.
- **2026-08-10 — root-caused why "rerun the rollup" alone would NOT have fixed dex_pools/dex_swaps/KAMINO_LENDING/
  BLAZESTAKE, fixed it, and re-scoped the remaining non-canonical work after reading this doc's own prior (found- stale)
  investigations.** Operator screenshot of the live Distinct Values panel (venues 32/102 non-canonical, instrument_types
  1/16, data_types 4/33, chains 1/23) triggered this pass.
  - **Panel mechanism corrected**: the panel (`deployment-ui` `DistinctValuesPanel.tsx` →
    `GET /distinct-values/{asset_group}` → `deployment-api/routes/data_status/_distinct_values.py`) does NOT read
    `_axis_census.py`'s live-query endpoint at all (that endpoint's capture_status fix from 2026-08-09,
    `deployment-api@3d7c7759c`, is real but irrelevant to THIS panel) — it reads a pre-computed nightly
    `gs://{project}-honest-coverage/{date}/coverage.json` rollup produced by
    `instruments-service/scripts/measure_honest_coverage.py`.
  - **Root cause found + fixed**: `measure_honest_coverage.py`'s `by_venue`/`by_venue_data_type`/
    `by_venue_instrument_type`/`by_chain` dicts — the ONLY consumer of which is this exact distinct-values endpoint
    (verified via a full-codebase grep, no other reader touches these 4 keys) — were built by grouping the FULL,
    unfiltered manifest dataframe, with no `capture_status` exclusion anywhere. A retired (`attempted_failed`) row's
    venue/data_type/instrument_type/chain name stayed a dict KEY regardless of status, so a fully-retired legacy value
    never disappeared from the enumeration no matter how many times the rollup reran — the exact same bug class already
    fixed once in `_axis_census.py` (2026-08-09), now closed at the actual SOURCE this panel reads. Fixed by filtering
    to `capture_status != "attempted_failed"` before those 4 specific groupby blocks only (NOT
    `ag_counts`/Layer-1/`by_day`/`by_venue_instrument_type_data_type` — those legitimately want the full breakdown;
    confirmed via the same reader-scope grep). Existing test `test_by_venue_instrument_type_correct_counts` asserted the
    OLD (buggy) behavior — updated its expectations + added
    `test_by_venue_instrument_type_attempted_failed_only_key_excluded` covering the actual fix (a key with ONLY
    attempted_failed rows must vanish entirely, not just zero its counts while surviving). Shipped
    `instruments-service@8b59e8ba2`.
  - **24 composite `PROTOCOL-CHAIN` venues (BALANCER-ETHEREUM, UNISWAP_V3-ARBITRUM, CURVE-ETHEREUM, etc.) — CONFIRMED
    already resolved, NOT a bug, no action.** This doc's own row 3 (2026-08-05) already root-caused this via a VM-run
    provenance trace: the writer is `market-data-processing-service`'s already-completed one-time
    `backfill_defi_dex_pool_swaps_source_correction.py` campaign (813,150 objects, 0 errors) — real backing data under
    MDPS's own `processed_candles/` path convention, not MTDS `raw_tick_data/` drift. "No fold, no purge, nothing to
    execute" (see `plans/active/issues/defi_underscored_multichain_composite_venue_fold_2026_08_04.md`, status:
    resolved). Live-verified magnitude matches (~227-237 rows/venue, 100% `captured`, consistent with this being real,
    intentional, low-volume MDPS output, not legacy drift debris). **This eliminates 75% of the 32 flagged non-canonical
    venues from the worklist entirely** — they will keep showing non-canonical on this panel (genuinely different naming
    convention, not something to fold), which is correct/expected, not a defect.
  - **`POOL` (uppercase) instrument_type — retirement candidate, but NOT SAFE TO RUN YET (moving-target corpus).** The
    2026-08-05 `fold_pool_instrument_type_casing_2026_08_05.py` run (this doc's row 2) was SCOPED ONLY to
    `dex_pool_swaps`/`dex_swaps` and is genuinely "0 remaining captured POOL" for that scope as verified then.
    Live-verified NOW: **`dex_pool_swaps` alone currently shows 1,574,108 `captured` POOL rows** (100% of the live
    total; no other data_type carries POOL) — meaning EITHER new POOL-cased data has landed in `dex_pool_swaps` since
    2026-08-05, or (more likely) the currently-running rebuild VM is surfacing pre-existing GCS objects whose hive path
    segment is genuinely `instrument_type=POOL` that the manifest hadn't made visible before this rebuild walked those
    dates. **Two back-to-back live queries ~10 minutes apart measured 1,323,097 then 1,574,108** — the count is GROWING,
    not stable, confirming the corpus is an active moving target while the rebuild VM
    (`canonical-migration-defi-rebuild-20260809-163511`) is still running and the consolidator is still merging its
    per-VM shards. Note `rebuild_defi_manifest.py`'s own `parse_hive_path` already lowercases every `instrument_type` it
    derives (`p["itype"].lower()`), so the rebuild's OWN new writes cannot be producing fresh uppercase POOL rows — the
    growth must be pre-existing historical rows becoming index-visible via consolidation, not a live writer bug. **Do
    NOT run a POOL retirement (or trust any snapshot count) until the rebuild VM reaches terminal state** — a direct-CAS
    full-index-rewrite retirement (same profile as the dex_pools/dex_swaps retirements this epic) racing an
    actively-merging consolidator is exactly the hazard this epic already paused the consolidator for twice this
    session; retiring against a still-growing population risks retiring an incomplete/stale snapshot and having to redo
    it.
  - **`dex_pool_fees` — reader repoint DONE, retirement status unconfirmed, tiny scope regardless.** This doc's row 5
    (sub-agent, DONE): the corpus was 0 objects for its entire lifetime (phantom manifest rows only, never real backing
    data) — `strategy-service`'s reader was repointed off it (`strategy-service@f7ca12767`), the materializer script
    deleted. A prior live read noted "dex_pool_fees shows 21" rows still counting on the (now- fixed) axis-census panel
    — unclear if those 21 rows were ever capture_status-retired. Low priority given the tiny scope; check
    post-VM-completion alongside POOL/rate_indices.
  - **`rate_indices` — retirement candidate, same moving-target caution as POOL.** Fold "GENUINELY 100% COMPLETE
    (2026-08-07)" per this doc's row 4 update, but per the now-established "fold ≠ retirement" pattern (the
    dex_pools/dex_swaps lesson from earlier this session), the legacy pre-fold rows were never capture_status- retired.
    Magnitude not yet re-measured live — defer measurement + retirement to the same post-VM-completion pass as POOL, for
    the same consolidator-race safety reason.
  - **`<blank>` instrument_type — mostly a panel false-positive, tiny real gap.** Live-verified: 5,352,845 raw
    blank-instrument_type rows, but **5,352,476 (99.99%) are `empty_confirmed`** (legitimate honest-absence markers — an
    empty_confirmed row correctly has no instrument_type since there's no actual data to type; heavily concentrated
    across ~15 data_types at a remarkably uniform ~212,800-215,600 each, consistent with one empty-marker per missing
    (date × venue × chain) cell, not a writer bug). Only **58 `captured`** rows (+ 268 `attempted_failed`, 43
    `expected_unattempted`) represent a genuine gap — real captured data missing its instrument_type. The panel's
    `<blank>` badge doesn't distinguish `empty_confirmed` from `captured` blanks, so it's currently over-reporting a
    ~5.3M-row "finding" that's actually a ~58-row one. Not fixed this pass (lower priority than the enumeration-key bug
    above; would need its own `_distinct_values.py`/rollup change to exclude `empty_confirmed` from the blank check
    specifically).
  - **`ASTER`/`GMX`/`HYPERLIQUID`/`EXTENDED`/`LIGHTER` venues + `HYPERLIQUID` chain + `AAVEV3` bare-alias historical
    rows** — status unchanged from earlier this epic: genuinely new/unresolved work (venue registration decisions,
    `[OPERATOR]`-gated historical purge respectively), not touched this pass.
  - **REVISED completion sequencing (supersedes earlier same-day framing)** — do NOT attempt POOL/rate_indices/
    dex_pool_fees retirements before the rebuild VM reaches terminal state; the corpus is actively moving and a
    retirement now risks acting on a stale/incomplete snapshot:
    1. ✅ `measure_honest_coverage.py` fix shipped (this entry).
    2. ⏳ Rebuild VM (`canonical-migration-defi-rebuild-20260809-163511`) reaches terminal — monitor via its `run.log`
       `Rebuild complete:` line; a `CronCreate` one-shot check is already scheduled ~06:43 local 2026-08-10
       (session-only, dies with the scheduling session — if it fires, whichever session picks it up should re-read this
       entry for full context first).
    3. Once terminal: re-measure POOL/rate_indices/dex_pool_fees live counts (must be STABLE across two queries a few
       minutes apart before trusting them); pause the manifest consolidator cron
       (`uts-prod-manifest-consolidator-market-data-defi-cron`, `asia-northeast1`); retire each via the proven
       reversible `capture_status: captured→attempted_failed` pattern (mirror
       `retire_dex_pools_legacy_captured_rows_2026_08_05.py`/`retire_dex_swaps_legacy_captured_rows_2026_08_09.py`);
       resume the consolidator.
    4. Trigger a fresh `measure_honest_coverage.py` rollup run.
    5. Re-check the Distinct Values panel — expect venues down to the genuinely-unresolved set (ASTER/GMX/
       HYPERLIQUID/EXTENDED/LIGHTER + the 24 composite venues, which are CORRECTLY non-canonical-but-accepted, not a
       bug), data_types clean (dex_pools/dex_swaps/rate_indices/dex_pool_fees all retired), instrument_types clean
       modulo the small genuine `<blank>` gap.
- **2026-08-10 (prose-findings formalization sweep)**: converted 3 prose findings into 3 formal todos (0 already
  resolved, so nothing cited-as-resolved-inline); added a `## Todos` section for the POOL/rate_indices/dex_pool_fees
  retirement sequencing (gated on `defi_track01`'s R3 rebuild VM, not duplicating that doc's own tracking), the
  `spot_pair` canonical-naming cross-check, and the Distinct Values panel's `<blank>` instrument_type over-report fix.
  Every other prose item in this doc's Deferred-work table and Progress Log already has its own delegated issue doc
  carrying real `- [ ]` checkboxes (verified via direct grep of each cited doc) and was left untouched to avoid
  duplication.
- **na-eligibility-audit 2026-08-10 (formalized-docs follow-up)**: KEEP-NA, valid — not all 3 open todos are bounded.
  Todo 1 (POOL/rate_indices/dex_pool_fees retirement) explicitly gates on `defi_track01_per_instrument_and_canon_id_2026_07_24.md`'s
  R3 rebuild VM reaching a stable terminal state — live-reverified this pass: R3 is still `[~]` (not terminal), the
  named successor VM `canonical-migration-defi-rebuild-20260809-163511` is confirmed GONE
  (`gcloud compute instances list --filter="name~canonical-migration-defi-rebuild"` returns empty per that doc's own
  2026-08-10 Progress Log), flagged there for root-cause triage, unresolved. A depends-on-a-still-open-prerequisite
  citation is KEEP-NA per this audit's own rule — since not every todo is bounded, the whole doc stays NA (todos 2/3
  are individually small and plausibly bounded, but the whole-doc bar requires all three).
