---
doc_type: issue
title: MTDS + UAC adapter-contract-call baseline regression (4 files, warn-only)
summary:
  check_adapter_contract_regression (STEP 5.70, warn-only) reports 4 files below their committed contract-call baseline
  — book_microstructure_handler.py missing/renamed, perp_funding_handler.py 9<10, and two unified-api-contracts
  crosscutting files below baseline. Pre-existing on committed HEAD, not caused by this session's Deribit migration
  work.
status: resolved
nature: record
asset_group: [cross-cutting]
stage: [meta]
repos: [market-tick-data-service, unified-api-contracts, unified-trading-pm]
scope: [engineer, admin]
tags: [adapter-contract, baseline-regression, warn-only]
related: [mtds_adapter_contract_baseline_regression_2026_06_24]
created: 2026-07-09
parent_epic: instruments_master
assigned_vm: NA
resolved_by:
  unified-trading-pm@ba098a7cc, unified-trading-pm@3aff7f716 (baseline regen + book_microstructure_handler.py cite);
  re-verified 2026-07-25 (this pass) — mtds@a4fb3d13, mtds@ba6df0ac, uac@06edd868
source: [market-tick-data-service quality-gates.sh STEP 5.70 check_adapter_contract_regression (warn-only post-gate)]
priority: P2
execution_scope: orchestrator-agent
drift_direction: advance-code
depends_on: []
locked_by: live-defi-rollout
locked_since: 2026-05-21
---

## RESOLVED 2026-07-25 — all 4 originally-flagged files confirmed non-regressions; no code change needed

**Verdict: none of the 4 files this doc flagged (2026-07-09) is a live regression today.** Independently of this doc,
`unified-trading-pm@ba098a7cc` (2026-07-13 — 4 days after this doc was filed) already diagnosed and closed all 4 via a
surgical baseline regen, citing the exact refactor/retirement commits per file (see "## Todos" below for the per-file
re-verification performed in this session). This doc had simply never been updated to reflect that 2026-07-13 closure,
so it sat open + narrative-only (no checkboxes, blank `assigned_vm`) for 12 days after its underlying finding was
already fixed. This pass (a) added the missing `## Todos` section, (b) re-verified each of the 3 files this doc still
listed as open (`perp_funding_handler.py`, `honest_coverage.py`, `source_priority.py`) against the LIVE baseline + LIVE
file contents (not just trusting the 2026-07-13 commit message), and (c) confirmed
`check_adapter_contract_regression.py --workspace-root <ws>` exits 0/OK for all three today. No code change needed in
any repo.

**Archival status — BLOCKED on operator `[unlock-plan]` approval, not done this pass**: per
`/codex/11-project-management/issue-doc-lifecycle.md` trigger 2, a resolved issue should archive immediately
(`plans/active/issues/` → `plans/archive/issues/`). This doc's frontmatter carries `locked_by: live-defi-rollout`
(`locked_since: 2026-05-21`), and `scripts/quality-gates.sh`'s locked-plan-deletion gate refuses to let an agent delete
a `locked_by`-carrying file from `plans/active/` without an explicit `[unlock-plan]` commit-message tag — CLAUDE.md is
equally explicit that adding `[unlock-plan]` requires asking the operator first, never doing it autonomously. An
attempted `git mv` to archive in this pass was reverted for exactly this reason (see Progress Log). So this doc stays
`status: resolved` in `plans/active/issues/` for now — a deliberate, surfaced exception to the issue-doc-lifecycle
archive-immediately rule, not a silent dual-track. **Follow-up**: operator confirms `[unlock-plan]` is safe here (all 4
todos below are closed, nothing is "actively being implemented") → `git mv` to `plans/archive/issues/` with
`[unlock-plan]` in the commit message.

## Progress Log

- **2026-07-25**: added the missing `## Todos` section; diagnosed all 3 remaining open files (`perp_funding_handler.py`,
  `honest_coverage.py`, `source_priority.py`) — all resolved-no-action (baseline already regenerated by a prior commit,
  re-verified live); re-confirmed the `book_microstructure_handler.py` 2026-07-12 correction footnote is still accurate;
  set `status: resolved` + `resolved_by:`. Attempted to archive (`plans/active/issues/` → `plans/archive/issues/`) per
  the issue-doc-lifecycle SSOT — `quality-gates.sh`'s locked-plan-deletion gate blocked it
  (`locked_by: live-defi-rollout` present, no `[unlock-plan]` tag) — reverted the move rather than add `[unlock-plan]`
  autonomously (CLAUDE.md: that tag requires asking the operator first). Doc ships resolved-in-place; archival deferred
  to an operator-approved follow-up.

## Todos

- [x] 1. ✅ [BACKEND] P2. Diagnose
      `market-tick-data-service/market_tick_data_service/cli/handlers/perp_funding_handler.py` (baseline 9) vs live
      contract-call count — confirm legit refactor vs regression, fix if regression. **Verdict: resolved-no-action** —
      live count = 9 (== baseline, not below). Baseline was already lowered 10→9 by `unified-trading-pm@ba098a7cc`
      (2026-07-13) citing `market-tick-data-service@ba6df0ac` ("retire standalone perp_funding for
      HYPERLIQUID/ASTER/PACIFICA-SOLANA/LIGHTER-ZKSYNC in favor of `derivative_ticker.funding_rate`", 2026-07-08) — a
      deliberate venue-consolidation retirement, not a silent drop. Re-verified this pass: `grep -c` over the 8 tracked
      patterns on live HEAD = 9; file still classifies + emits honest absence on every remaining fetch path
      (`record_captured`/`record_zero_rows`/`record_failed`/`record_empty` all present, non-comment call-sites confirmed
      by direct read). No code change needed. Evidence: `unified-trading-pm@ba098a7cc` (prior fix) + this pass's live
      re-verification (2026-07-25, no new commit — no code changed).
- [x] 2. ✅ [BACKEND] P2. Diagnose
      `unified-api-contracts/unified_api_contracts/canonical/crosscutting/honest_coverage.py` (baseline 38) vs live
      contract-call count — confirm legit refactor vs regression, fix if regression. **Verdict: resolved-no-action** —
      live count = 38 (== baseline, not below). Baseline was already lowered 41→38 by `unified-trading-pm@ba098a7cc`
      (2026-07-13) citing `unified-api-contracts@06edd868` ("split
      mvp_scope/honest_coverage/source_priority/tradfi_ticker_universe under the 900-line file-size QG cap") — 3 calls
      relocated to the new `_honest_coverage_empty_reasons.py` (now separately baselined at 3), not dropped. Re-verified
      this pass: `_honest_coverage_empty_reasons.py` (baseline 3), `_honest_coverage_clusters.py` (baseline 4), and
      `_honest_coverage_logic.py` (baseline 11) all exist on disk and carry contract calls — the split landed cleanly,
      nothing lost. No code change needed. Evidence: `unified-trading-pm@ba098a7cc` (prior fix) + this pass's live
      re-verification (2026-07-25, no new commit).
- [x] 3. ✅ [BACKEND] P2. Diagnose
      `unified-api-contracts/unified_api_contracts/canonical/crosscutting/source_priority.py` (baseline 1, originally
      reported "file missing or renamed") vs live state — confirm legit refactor vs regression, fix if regression.
      **Verdict: resolved-no-action** — the baseline entry for this exact file no longer exists at all (removed by the
      same `ba098a7cc` regen, since `write_baseline()` only persists entries with `observed > 0`); the file's 1
      tracked-pattern occurrence moved to the new `_source_priority_provenance.py` (baseline 1, confirmed present +
      still carrying the reference). Re-verified this pass by reading `source_priority.py` directly: it is a pure
      `SOURCE_PRIORITY` registry/lookup module (no fetch/classify logic of its own — it never had a real
      adapter-contract obligation; the original baseline=1 entry was 1 incidental docstring/comment mention, now
      relocated). `check_adapter_contract_regression.py     --workspace-root <ws>` run live 2026-07-25 reports OK/exit 0
      with no failure for this file (it's simply not baseline-tracked anymore — correctly so). No code change needed.
      Evidence: `unified-trading-pm@ba098a7cc` (prior fix) + this pass's live re-verification (2026-07-25, no new
      commit).
- [x] 4. ✅ [BACKEND] P2. Re-verify the 2026-07-12 correction footnote on `book_microstructure_handler.py` (the 4th
      originally-flagged file) is still accurate before closing this doc. **Verdict: confirmed still accurate** — the
      file's baseline entry was itself removed from `adapter_contract_baseline.yaml` by `unified-trading-pm@ba098a7cc`
      (2026-07-13), which cites the same retirement this doc's footnote already named
      (`market-tick-data-service@a4fb3d13`, "retire order_flow_imbalance feature — zero real consumers, zero production
      rows ever captured, duplicated MDPS's live implementation", 2026-07-07). No open action; the footnote was correct
      and the baseline is now consistent with it. Evidence: `unified-trading-pm@ba098a7cc`.

## What I found

While running `market-tick-data-service`'s `quality-gates.sh` to verify an unrelated, scoped fix (`deribit_ws.py`
live-classification bug, see `unified-trading-pm/docs` cross-link in `instruments-service/docs/CEFI_INSTRUMENTS.md`
"Live-vs-batch classification bug"), STEP 5.70 (`check_adapter_contract_regression`, warn-only — does NOT fail QG, exit
stays 0 per the 2026-06-24 precedent `plans/archive/issues/mtds_adapter_contract_baseline_regression_2026_06_24.md`)
reported 4 files below their committed adapter-contract-call baseline:

- `market-tick-data-service/market_tick_data_service/cli/handlers/book_microstructure_handler.py`: 0 contract calls <
  baseline 8 (**file missing or renamed** — does not exist on disk at HEAD). **Correction (2026-07-12,
  doc-reconciliation finding 121, §A2 "50 reclassified" blanket ruling): this is NOT an open regression needing
  diagnose-before-fix/restore — it is the already-confirmed, deliberate 2026-07-07 retirement of MTDS's
  `order_flow_imbalance` feature** (`mtds_mdps_order_book_imbalance_duplicated_2026_07_07.md:141-150`,
  `market-tick-data-service@a4fb3d13`, blast-radius-verified zero real consumers + zero production rows ever captured,
  `quality-gates.sh` green). The baseline entry should be regenerated/dropped from `adapter_contract_baseline.yaml` to
  reflect the retirement rather than treated as a mystery deletion; not applied here (that file is outside this
  doc-reconciliation pass's scope).
- `market-tick-data-service/market_tick_data_service/cli/handlers/perp_funding_handler.py`: 9 contract calls < baseline
  10
- `unified-api-contracts/unified_api_contracts/canonical/crosscutting/honest_coverage.py`: 38 contract calls < baseline
  41
- `unified-api-contracts/unified_api_contracts/canonical/crosscutting/source_priority.py`: 0 contract calls < baseline 1
  (file missing or renamed)

Tracked patterns:
`classify_venue_error | ADAPTER_FETCH_FAILED | record_captured | record_empty | record_zero_rows | record_failed`.
Baseline SSOT: `unified-trading-pm/scripts/quality_gates/adapter_contract_baseline.yaml`.

**Confirmed pre-existing, not caused by this session**: `git status --short` is clean for all 4 files in both repos at
the time of this finding (2026-07-09) — this is the committed HEAD state, not uncommitted WIP from this or any
concurrent session. This session's own diff touched only
`market-tick-data-service/market_tick_data_service/live/ connectors/deribit_ws.py` + its test file, and
`instruments-service` (catalog/by-date migration scripts + docs) — none of the 4 flagged files.

`book_microstructure_handler.py` and `source_priority.py` both being reported as "file missing or renamed" (0 real
contract calls, both below a nonzero baseline) suggests these files were deleted/renamed in a past commit without the
baseline being updated to match — the same failure mode as the 2026-06-24 precedent, but for different files. Not
independently re-diagnosed here (out of scope for the Deribit migration task that surfaced it) — needs the same
diagnose-before-fix treatment as the linked precedent: confirm whether each is a legitimate refactor (calls moved
elsewhere, baseline should be regenerated) or a real regression (contract calls actually dropped, should be restored).

## Why it matters

Same rationale as the linked 2026-06-24 precedent — this ratchet exists to catch the lint-sweep class of bug
(`lint_sweep_774602ea8_regression_audit_2026_05_20.md`: a sweep silently wiped 31 contract calls from kalshi.py +
polymarket_clob.py). A genuine drop below baseline means an adapter may no longer classify errors / emit
`ADAPTER_FETCH_FAILED` / record honest absence on every path — a data-pipeline correctness risk.

## Recommended decision

Diagnose-before-fix (read both sides) per file, then either regenerate the baseline (if calls legitimately moved) or
restore the missing contract calls (if a real regression). Non-urgent (warn-only, does not block QG/commits), but a
data-correctness-adjacent item that should not sit indefinitely — same owner class as the 2026-06-24 precedent.

**Executed** — see the "## RESOLVED 2026-07-25" banner + "## Todos" above: all 4 files landed on the "legit
refactor/retirement, calls moved or genuinely retired" branch, none on the "real regression" branch. No contract calls
were restored because none were actually dropped.
