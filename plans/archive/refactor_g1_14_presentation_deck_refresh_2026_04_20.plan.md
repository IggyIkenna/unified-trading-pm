---
doc_type: plan
title: Refactor G1.14 — Presentation deck refresh (14-item G1 + 7 new slides + optional HTML stretch)
summary:
status: complete
nature: record
asset_group: [cross-cutting]
stage: [meta]
repos: [unified-trading-pm, unified-trading-system-ui]
scope: [engineer, admin]
tags: []
related: []
created: "2026-04-20"
priority: P0
owner: agent
locked_by: live-defi-rollout
locked_since: 2026-04-20
depends_on:
  [
    /codex/14-playbooks/infra-spec/stage-3e-refactor-plan.md §1.14,
    /codex/14-playbooks/presentations/target-experience-post-refactor.md,
  ]
---

## Deferred work — migrated to:

**None** — successor: not applicable. Plan archived as 100% completed (no open `- [ ]` items at archive time). Any
incidental DEFERRED / post-cutover / out-of-scope tokens in the body are historical context, not unfinished work.

# Refactor G1.14 — Presentation deck refresh

## Context

Stage 3E §1.14 (added 2026-04-20): refresh the existing
`/codex/14-playbooks/presentations/target-experience-post-refactor.md` which was drafted against the prior 9-item G1
scope. Seven new slides must land to reflect the 2026-04-20 amendments (items 1.10, 1.11, 1.12, 1.13, 1.4 expansion, MCP
Playwright discipline, dev/staging parity). Current deck has slides for the original 9 items + screenshots generated by
the prior Playwright run. Stretch goal: ship a reveal.js HTML wrapper at
`presentations/target-experience-post-refactor.html` so the deck is viewable as slides (not just markdown); this stretch
depends on G1.4 completing so its new persona screenshots exist to be embedded.

## Decisions locked with user (2026-04-20)

| Decision                                     | Chosen                                                                                                  | Source                       |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ---------------------------- |
| Markdown refresh is standalone (Wave A)      | Does not depend on other G1 items                                                                       | Kickoff §1.14                |
| HTML stretch depends on G1.4 new screenshots | Ships after G1.4 has new personas rendered                                                              | Kickoff §1.14 Wave F stretch |
| 7 new slides are additions, not replacements | Existing 9-item slides stay (updated where outdated); 7 new slides append                               | Kickoff §1.14                |
| Screenshot regeneration deferred to G1.4     | This plan references `presentations/screenshots/` as-is; G1.4's plan owns regen after new personas land | Kickoff §1.14                |

## Cross-references

- **Sibling Wave A plans:** refactor*g1*{1,3,5,9,12}\_2026_04_20.md — all 5 directly relevant as they're the other Wave
  A items that land before this deck's markdown refresh is meaningful.
- **Wave F dependency (HTML stretch):** `refactor_g1_4_persona_combinatorial_expansion_2026_04_20.md` — provides new
  personas → new screenshots.
- **Source deck:** `/codex/14-playbooks/presentations/target-experience-post-refactor.md`
- **Existing screenshots:** `codex/14-playbooks/presentations/screenshots/` — 7 files (admin-dashboard.png,
  admin-strategy-catalogue.png, anon-home.png, client-full-dashboard.png, prospect-im-briefings.png,
  prospect-im-dashboard.png, prospect-im-reports-overview.png)
- **Every sibling refactor_g1_X plan** — each of the 14 G1 items gets a ≥1-slide treatment in the deck.

## Mandatory read-set

1. `/codex/14-playbooks/infra-spec/stage-3e-refactor-plan.md` (full — especially §1.14 amendment)
2. `/codex/14-playbooks/presentations/target-experience-post-refactor.md` (full — the current deck)
3. `codex/14-playbooks/presentations/screenshots/` — list all existing screenshots
4. All 13 sibling `plans/active/refactor_g1_*_2026_04_20.md` files (to extract each item's deliverable statement for
   slide content)

## Out of scope

- Regenerating screenshots for the new personas — G1.4's plan owns that; this plan cites existing screenshots + leaves
  placeholders for new ones.
- Building the reveal.js engine from scratch — use a standard reveal.js or similar boilerplate.
- New visual design — keep Bloomberg-style aesthetic per `cross-cutting/bloomberg-style-aesthetic.md`.
- New presentation for different audiences — this is the engineering-facing target-experience deck; sales collateral is
  a different deck under G2.

## Phase breakdown

### Phase 14A — Refresh existing slides for the 9-item G1 → 14-item G1 shift

- [x] [AGENT] P0. Re-read `target-experience-post-refactor.md` in full; identify slides mentioning "9 G1 items" or
      equivalent counts that must bump to 14.
- [x] [AGENT] P0. Update counts + any summary table + any mermaid DAG that enumerates items.
- [x] [AGENT] P0. Update Wave grouping summary (was A/B/C — now A/B/C/D/E/F).

### Phase 14B — Add 7 new slides

- [x] [AGENT] P0. Slide N: **G1.10 questionnaire flow** — prospect questionnaire UI + admin playback + dimensions table.
- [x] [AGENT] P0. Slide N+1: **G1.11 service-family scope rules** — `observe ∈ {DART}` /
      `reporting ∈ {IM, DART-reporting-only, Reg Umbrella}` / etc.
- [x] [AGENT] P0. Slide N+2: **G1.12 public-site IA polish** — before/after SiteHeader consistency.
- [x] [AGENT] P0. Slide N+3: **G1.13 upsell tempt-logic** — vague-answer → widen profile → tighter-answer → tighten.
- [x] [AGENT] P0. Slide N+4: **G1.4 combinatorial persona matrix** — 15-20 personas × questionnaire axes.
- [x] [AGENT] P0. Slide N+5: **MCP Playwright test discipline** — MCP during dev + durable spec for CI +
      quality-gates.sh hook; one slide.
- [x] [AGENT] P0. Slide N+6: **dev/staging parity** — the rule + what differs (only auth source) + what must match
      (personas, restriction profiles, questionnaire).

### Phase 14C — Update deck metadata + cross-links

- [x] [AGENT] P0. Update deck title, version, date to "v2 — 2026-04-20, post 14-item G1 amendment".
- [x] [AGENT] P0. Cross-link each slide to its corresponding `plans/active/refactor_g1_*_2026_04_20.md` file.
- [x] [AGENT] P0. Update any superseded table (e.g. "Phases → deliverables").

### Phase 14D — Optional HTML stretch (AFTER G1.4 lands)

- [x] [AGENT] P1. Drop a reveal.js boilerplate at `presentations/target-experience-post-refactor.html` consuming the
      markdown as slide source.
- [x] [AGENT] P1. Embed the 7 existing screenshots + placeholder slots for new G1.4 persona screenshots.
- [x] [AGENT] P1. Verify deck renders locally (`open presentations/target-experience-post-refactor.html`).
- [x] [AGENT] P1. Commit HTML separately — do not block the markdown commit on the HTML.

### Phase 14E — Verify

- [x] [SCRIPT] P0. Markdown lints clean (no broken cross-references — run
      `rg "\[.*\]\(.*\)" target-experience-post-refactor.md` + spot-check each link resolves). 57 internal links
      validated 2026-04-20; zero broken.
- [x] [SCRIPT] P0. PM QG green.

## Critical files to be modified

- `/codex/14-playbooks/presentations/target-experience-post-refactor.md` — MODIFY (refresh + append 7 slides)
- `codex/14-playbooks/presentations/target-experience-post-refactor.html` — NEW (stretch, post-1.4)
- `codex/14-playbooks/presentations/screenshots/` — UNCHANGED in this plan (G1.4 owns regen)

## Execution DAG

```
14A (refresh existing)  →  14B (add 7 slides) + 14C (metadata)  [parallel]  →  14E (verify)
                                                                                    ↓
                                                                            14D (HTML stretch, blocked on G1.4)
```

## Verification

1. Deck mentions "14 G1 items" (not 9) throughout.
2. 7 new slides present with the stated titles.
3. Every one of the 14 G1 items has ≥1 slide reference.
4. Every slide cross-links to its `refactor_g1_*_2026_04_20.md`.
5. PM QG green.
6. (If HTML stretch landed) deck renders in a browser with screenshots visible.

## Handoff

Unblocks:

- **G2 planning sessions** — presenter has a fresh, accurate deck to walk through G1 completion → G2 scope transition.
- **G1.4 screenshot regen (Wave F)** — the HTML deck can embed new screenshots as they land.
- **Sales collateral (future G2)** — derived deck variants can fork from this engineering-facing source.

## Playwright test coverage (mandatory)

**MCP Playwright during dev:** For the markdown-only phase, Playwright is not directly exercising a UI; however, if the
HTML stretch lands, drive `file://<path-to-HTML>` through MCP Playwright to verify slides render + screenshots load +
keyboard navigation works. For the markdown, Playwright spec instead shells out to a link-check tool + asserts
cross-references resolve.

**Durable spec for CI:**
`unified-trading-system-ui/tests/e2e/playbooks/refactor/refactor-g1-14-presentation-deck.spec.ts` — must:

1. Seed an `admin` persona via `tests/e2e/playbooks/seed-persona.ts` (deck is admin-audience).
2. (Markdown phase) shell-out to run a markdown link-checker on
   `/codex/14-playbooks/presentations/target-experience-post-refactor.md`; assert exit 0.
3. (HTML stretch, if landed) load the HTML via MCP Playwright tools, verify slide count ≥ 16 (existing 9+ and 7 new),
   verify screenshots render (non-zero bounding box), verify keyboard arrow navigation advances slides.
4. Assert visibility-slicing vs G1.6 `access_control` formula once G1.6 lands; deck is admin-audience so admin sees all;
   stub until G1.6 lands.
5. Include orphan-reachability assertion — every slide references a plan file that exists under `plans/active/`.
6. Wired into `scripts/quality-gates.sh` Playwright step.

## AGENT EXECUTION PROMPT

**Copy-paste everything below this line into a new agent session to execute Refactor G1.14 (Wave A markdown refresh —
standalone. Wave F HTML stretch — depends on G1.4 completing).**

---

You are executing **Refactor G1.14 — Presentation deck refresh** for the Unified Trading System at Odum Research.
Markdown phase = Wave A (parallelisable with 1.1, 1.3, 1.5, 1.9, 1.12). HTML stretch = Wave F (after G1.4 lands new
personas → new screenshots).

### Pre-flight check

```
cd /Users/ikennaigboaka/Code/unified-trading-system-repos/unified-trading-pm
git checkout live-defi-rollout && git pull
ls /codex/14-playbooks/presentations/target-experience-post-refactor.md
ls codex/14-playbooks/presentations/screenshots/   # expect 7 pngs
ls plans/active/refactor_g1_*_2026_04_20.md 2>&1 | wc -l   # expect 14 once all plans land; safe to run partial on the first 13
```

All must exist. STOP if any missing.

### Mandatory rules injection

- Read
  `/Users/ikennaigboaka/Code/unified-trading-system-repos/unified-trading-pm/cursor-configs/SUB_AGENT_MANDATORY_RULES.md`
- Read `/Users/ikennaigboaka/Code/unified-trading-system-repos/unified-trading-pm/plans/PLAN_FORMAT.md`
- `WORKSPACE_ROOT = /Users/ikennaigboaka/Code/unified-trading-system-repos/`

### Task

Execute every checkbox in Phases 14A through 14E of this plan (14D is optional; blocks on G1.4):
`plans/active/refactor_g1_14_presentation_deck_refresh_2026_04_20.md`

### Read-set (mandatory)

Paths in the plan's "Mandatory read-set" — all 4.

### Deliverables

- Modified: `/codex/14-playbooks/presentations/target-experience-post-refactor.md` (refresh + 7 new slides + metadata +
  cross-links)
- New (stretch, post-G1.4): `codex/14-playbooks/presentations/target-experience-post-refactor.html`
- New test: `unified-trading-system-ui/tests/e2e/playbooks/refactor/refactor-g1-14-presentation-deck.spec.ts`

### MCP Playwright clause (verbatim — REQUIRED)

For the markdown-only pass, the Playwright spec shells out to a markdown link-checker and asserts exit 0. For the HTML
stretch, drive `file://<path-to-HTML>` (or `localhost:3000/presentations/...` if served) through MCP Playwright tools
during dev to verify slides render, screenshots load, keyboard nav works. Commit the durable spec at
`unified-trading-system-ui/tests/e2e/playbooks/refactor/refactor-g1-14-presentation-deck.spec.ts` — seed admin persona
via `tests/e2e/playbooks/seed-persona.ts`, walk through the deck, assert slide count + screenshots + cross-reference
resolution + visibility-slicing stub, include orphan-reachability assertion on referenced plan files, wire into
`scripts/quality-gates.sh`.

### Commit strategy

PM repo — two commits (markdown + HTML stretch).

Markdown:

```
cd unified-trading-pm
bash scripts/quickmerge.sh "docs(codex/playbooks/presentations): G1.14 — deck refresh for 14-item G1 + 7 new slides" --agent --files "/codex/14-playbooks/presentations/target-experience-post-refactor.md"
```

HTML stretch (only after G1.4 landed):

```
bash scripts/quickmerge.sh "docs(codex/playbooks/presentations): G1.14 stretch — reveal.js HTML deck" --agent --files "codex/14-playbooks/presentations/target-experience-post-refactor.html"
```

Fallback if quickmerge blocked: manual `git add <files> && git commit -m "..." && git push origin live-defi-rollout`.
Never `--dep-branch`, never `git reset --hard`.

### Success criteria

1. ✅ Deck refreshed from 9-item G1 → 14-item G1 language throughout.
2. ✅ 7 new slides present (titles per Phase 14B).
3. ✅ Every G1 item has ≥1 slide + cross-link to its plan file.
4. ✅ Markdown link-check exit 0.
5. ✅ PM QG green.
6. ✅ (Stretch) HTML deck renders with keyboard nav.
7. ✅ Commit SHA(s) pushed to `origin/live-defi-rollout`.

### What NOT to do (verbatim guardrails)

- Do NOT read, cite, or derive anything from `_archived_pre_v2/` — v2 only.
- Do NOT `git reset --hard` or `git push --force`.
- Do NOT use `--dep-branch` flag; `--agent` only.
- Do NOT cherry-pick around unrelated WIP — multiple agents on `live-defi-rollout` concurrently is expected.
- Do NOT regenerate screenshots here — G1.4's plan owns regen for new personas.
- Do NOT modify `stage-3e-refactor-plan.md` — read-only from the 14-plan perspective.
- Do NOT remove existing slides — refresh + append only.
- Do NOT block the markdown commit on the HTML stretch — ship markdown first.

### Report back

- Before / after deck slide count.
- 7 new slide titles.
- Cross-link table (slide → plan file).
- Markdown link-check result.
- Playwright spec path + pass status.
- Commit SHA(s) pushed to live-defi-rollout.
- Any gaps or open questions for the user.
